# $bog_gamengine — план работ

Решения в [ARCHITECTURE.md](./ARCHITECTURE.md), правила кода в [STYLE.md](./STYLE.md).
Здесь порядок и приёмка. Каждая задача нарезана под одного молера, `effort: medium`.

Статус: `[ ]` не начата, `[~]` в работе, `[x]` принята.

Донор: пак `bog/game` (форк `hyoo-ru/game.hyoo.ru`). Перед этапами 1 и 3 прочитать
`bog/game/eye/eye.view.ts` целиком, это единственный живой потребитель `$mol_3d`.

---

## Этап 0. Спайки

Три проверки фундамента. Шли параллельно. Все три приняты 22.09.2026, находки
перенесены в архитектуру (разделы 1, 3.1, 3.1а, 3.7, 5, 7, 8, 14) и в STYLE.md. Папка
`spike/` удалена после приёмки задачи 1.9, отчёты остались в истории репозитория.
Образцы теперь живые модули: шейдер в TS это `shader/flat`, владелец GL это `draw`,
проба пикселя это `probe`.

- [x] **S1. Две программы с разными face в одном бандле. ПРИНЯТ.** Грабля шире, чем
  думали: падают обе программы, порядок не важен. Обход работает только с
  `$mol_3d_glsl_both` в префиксе. Вторая грабля: подчёркивание в `.glsl` сборщик читает
  как путь модуля. Отчёт спайка S1 в истории репозитория, коммит 2ad4921.

- [x] **S2. Десять тысяч инстансов при 60 кадрах. ПРИНЯТ.** Горячий буфер: 0.6 мс на
  10 000, 14 мс на 500 000, ломается на миллионе. Реактивный контроль: 14 мс уже на
  10 000, 153 мс на 50 000. Цена записи в атом около 1.3 мкс, аллокация ни при чём.
  Замер внутри `paint()` занижает кадр втрое, потому что `step` считает планировщик.
  Отчёт спайка S2 в истории репозитория, коммит 9643ddc.

- [x] **S3. WebGL в headless Chrome через `bog/probe`. ПРИНЯТ.** Со штатным
  `--disable-gpu` контекста нет, с `--use-angle=swiftshader` центр красный. В пробу
  добавлено поле `flags` (коммит `6b3c091` в `b-on-g/probe`). Нужны
  `preserveDrawingBuffer: true` и `ready` по `canvas.width > 0`. Прогон 0.7 с. Отчёт
  спайка S3 в истории репозитория, коммит 84a7f24, живой образец `probe/probe.node.ts`.

## Этап 1. Каркас

Цель: вращающийся цветной квад на экране, камера переключается между плоской и
объёмной, всё реактивно. Ни одной картинки.

- [x] **1.1. `clock`.** `frame()`, `dt()`, `time()`, `paused?`, `speed?`. `dt` режется
  сверху 0.1 с. **Приёмка:** тест в ноде с подменой `$mol_state_time.now`: три тика
  дают `frame 3`, пауза даёт `dt 0`, скачок времени на 5 с даёт `dt 0.1`.

- [x] **1.2. `vec`.** `vec2`/`vec3` как функции над `Float32Array` с выходным
  аргументом: `add( out, a, b )`, `scale`, `len`, `norm`, `dot`, `cross`, `lerp`.
  **Приёмка:** тест на каждую функцию, ни одной аллокации внутри (проверить глазами,
  в тесте `out === result`).

- [x] **1.3. `node`.** `pos?`, `rot?`, `scale?`, `parent?`, `trans()` из них,
  `world()` через родителя, `step( dt )` пустой, `kids()`. Поворот в плоском
  режиме это угол вокруг Z, в объёмном три угла; храним всегда три.
  **Приёмка:** тест: ребёнок со сдвигом 1 у родителя с поворотом π/2 даёт `world`
  с переносом `(0, 1, 0)` с точностью 1e-6.

- [x] **1.4. `cam`, `cam/flat`, `cam/deep`.** `view()` это инверсия `world()`.
  `flat.proj( aspect )` ортография по `zoom` и `pixels_per_unit`, `deep.proj( aspect )`
  перспектива по `fov`, `near`, `far`. **Приёмка:** тесты по разделу 14 архитектуры:
  точки на границах переходят в `±1`.

- [x] **1.5. `shader` и `shader/flat`.** База собирает программу из `face`, `vert`,
  `frag` с подмешиванием только `$mol_3d_glsl_both` (образец `shader/flat/flat.ts`).
  Первый шейдер: цветной квад без текстуры, `inst_trans`, `inst_tint`.
  **Приёмка:** тест в ноде: `vert()` и `frag()` содержат `main`, каждое имя из
  `face.input` встречается в `vert()`, `program()` не содержит `$mol_3d_glsl_vert`.

- [x] **1.6. `shape`, `shape/quad`.** База добавляет к `$mol_3d_shape` `normals()` и
  `count()`. Квад как `TRIANGLE_STRIP` из четырёх вершин, обход против часовой, чтобы
  cull не срезал. **Приёмка:** тест на длины массивов и порядок обхода.

- [x] **1.7. `batch`.** `shader`, `shape`, `nodes()`, плоские буферы `trans`
  (`16 × cap`), `tint` (`4 × cap`), `fill()` пишет `world()` каждого узла на место
  и возвращает `count`. Рост `cap` удвоением, буферы пересоздаются только при росте.
  **Приёмка:** тест: два узла дают `count 2` и правильные переносы в `trans` по
  смещениям 12 и 28; третий узел не аллоцирует, если `cap` хватает.

- [x] **1.8. `scene`.** Корневой `node`, `nodes()` обходом, `batches()`, `step()`
  мемо на `clock.frame()`: шаги 2–4 раздела 5 архитектуры. **Приёмка:** тест: узел с
  `step`, двигающим `pos` на `dt`, через три тика на `dt 0.016` сдвинут на 0.048.

- [x] **1.9. `draw`.** Наследник `$mol_3d_pane`, контекст с
  `preserveDrawingBuffer: true`. Кэш программ по шейдеру, VAO и буферов по батчу,
  `paint()` по разделу 7, `stat()` по разделу 5. Состояние GL перед каждым батчем
  выставляется явно. **Приёмка:** проба по рецепту раздела 14 (образец
  `probe/probe.node.ts`): квад с `inst_tint` красный, центр холста красный. Второй кадр не
  зовёт `createBuffer` (обернуть контекст прокси в пробе и посчитать). После приёмки
  папка `spike/` удаляется тем же коммитом.

- [x] **1.10. `demo`, первая страница.** Корень `$mol_book2` с одной страницей «Квад»:
  панель `draw` внутри `sub`, никогда не корнем (3.7). Сцена из `scene`, узел,
  вращающийся в `step`, переключатель камер `$mol_switch`, чекбокс паузы к
  `clock.paused?`, `draw.stat()` в подвале. Единственный `index.html` пака.
  **Приёмка:** пользователь открывает через дев-сервер, квад крутится, пауза
  останавливает, камера переключается без перезагрузки. `git ls-files | grep -c index.html` равно 1.

## Этап 2. Плоский мир

Цель: маленькая игра с видом сверху на тайловой карте, спрайты из атласа,
управление с клавиатуры, столкновения со стенами, счётчик на HUD.

- [x] **2.1. `atlas`.** `uris()`, `size`, загрузка через `$mol_3d_image` и
  `$mol_wire_race`, проверка квадрата и размера с внятной ошибкой, `layer( name )`,
  `ready()`. **Приёмка:** тест в ноде с подменой `data()`: картинка 64×32 в атласе
  64 даёт ошибку с именем файла; `layer( 'hero' )` равен индексу в `uris()`.

- [x] **2.2. `shader/sprite`.** Квад с `inst_layer`, `inst_uv_shift`, `inst_uv_scale`,
  `inst_tint`, `sampler2DArray`. Premultiplied blend. **Приёмка:** тест сборки
  строк как в 1.5; проба: спрайт с прозрачным углом не закрашивает фон.

- [x] **2.3. `sprite`.** Узел с `atlas`, `frame?` именем слоя, `layer?` для порядка,
  `tint?`, `flip_x?`. Регистрируется в батч своего атласа. **Приёмка:** тест:
  два спрайта разных атласов дают два батча, одного атласа один батч.

- [x] **2.4. `key`.** Действия по именам поверх `$mol_keyboard_state`:
  `bind` из дерева `{ jump: [ 'space', 'W' ] }`, `action( 'jump' )`. **Приёмка:**
  тест с прямым вызовом `down`/`up` состояния.

- [x] **2.5. `phys`.** `body` (`pos`, `vel`, `size`, `kind`, `static`), `tile` из
  строк символов с картой проходимости, `step( dt )`: интеграция, столкновение
  aabb×aabb и aabb×tile по минимальной оси, `on_hit`. **Приёмка:** тесты: тело
  летит в стену тайла и останавливается на её грани; два динамических тела
  расталкиваются поровну.

- [x] **2.6. `demo`, страница «Плоский мир».** Карта 20×15 из символов, стены и
  пол спрайтами, герой на WASD, три монеты, счётчик `$mol_meter` в HUD, звук
  подбора через `sound` (задача 4.2 может быть позже, тогда без звука).
  **Приёмка:** пользователь играет, монеты собираются, счётчик растёт, в стены не
  проходит. Проба: после `step` на 60 кадров с зажатой `D` `pos.x` героя больше
  стартового.

## Этап 3. Объёмный мир

Цель: комната из ящиков с текстурами, камера от первого лица, свет.
`bog/game` переезжает на движок и становится вторым потребителем.

- [x] **3.1. `shape/box`, `shape/plane`.** Индексированные или strip-геометрии с
  нормалями и uv на каждую грань. **Приёмка:** тест: у box 24 вершины и 6 разных
  нормалей.

- [x] **3.2. `shader/solid`.** `inst_trans`, `inst_layer`, нормаль через
  `mol_3d_mat4_scales`-подобную коррекцию, ламберт от одного направленного света
  `light_dir` юниформом, ambient. **Приёмка:** тест сборки строк; проба: грань,
  обращённая к свету, светлее противоположной.

- [x] **3.3. `mesh`.** Узел с `shape`, `atlas`, `frame?`, регистрируется в батч
  шейдер×фигура×атлас. **Приёмка:** тест как в 2.3.

- [x] **3.4. `cam/deep` управление.** Узел-контроллер от первого лица: `yaw`,
  `pitch` от `point`, движение по `key`. Живёт в `demo`, не в движке.
  **Приёмка:** проба: после `step` с зажатой `W` камера сместилась вдоль своего −Z.

- [x] **3.5. `demo`, страница «Комната».** Пол, потолок, стены ящиками из карты
  символов, свет, wireframe-чекбокс как в `bog/game`. **Приёмка:** пользователь
  ходит по комнате, стены не проходятся (aabb из `phys` в трёх осях).

- [x] **3.6. Перенос `bog/game/arcade` на движок.** `eye` удаляется, `arcade` собирает
  сцену из `mesh` и `cam/deep`, актёры остаются как были. Отдельный репозиторий, свой
  коммит. **Приёмка:** аркада выглядит как раньше, `objects`/`shapes`/`textures` в
  подвале совпадают со старыми числами.

## Этап 4. Ввод, звук, выбор

- [x] **4.1. `pad`.** Оси и кнопки по кадру, мёртвая зона, слияние с `key` в
  `input.action()` и `input.axis( 'move' )`. **Приёмка:** тест с подменой
  `getGamepads`.

- [x] **4.2. `sound`.** Комната, сэмплы по именам, `play( name )`, `loop`.
  **Приёмка:** демо издаёт звук подбора монеты после первого клика.

- [x] **4.3. `point`.** Экран→мир для плоской камеры, луч для объёмной,
  `pick()` перебором aabb узлов. **Приёмка:** тест: центр экрана при камере в
  начале даёт `(0, 0)`; проба: клик по монете в демо подбирает её.

- [x] **4.4. Подписи в мире.** `point.screen( pos )` и пример в демо: имя над
  героем как `$mol_view` с `position: absolute`. **Приёмка:** подпись едет за героем.

## Этап 5. Ресурсы и сеть

- [x] **5.1. Анимация спрайтов.** `sprite.clip?` с именем и `fps`, `frame`
  выводится из `clock.time()`. **Приёмка:** тест: через 0.5 с при `fps 4` кадр второй.

- [x] **5.2. glTF, минимум.** Позиции, нормали, uv, одна текстура, без скелета.
  `shape/gltf` из `ArrayBuffer`. **Приёмка:** тест на маленьком встроенном файле;
  демо показывает загруженную модель.

- [x] **5.3. Спайк сети на Гипер Базе.** Два окна, у каждого свой герой, `pos`
  в атоме ленда, второе окно видит движение первого. **Приёмка:** задержка на
  локальном мастере на глаз меньше 200 мс; отчёт с найденными граблями.

- [x] **5.4. Деплой демо.** `.github/workflows/deploy.yml` по образцу `bog/smalljs`,
  без пререндера, на `b-on-g.github.io/gamengine/`. **Приёмка:** страница открывается
  на телефоне, плоская демка играется с экранных кнопок (`$mol_button` в HUD пишут в
  те же действия `key`).

## Этап 6. Хвосты после переноса аркады

Перенос `bog/game/arcade` (задача 3.6, коммит `355ec89` в `b-on-g/game`) показал, чего
движку не хватает. Аркада живёт без этого, но каждый пункт это дыра в API.

- [x] **6.1. Билборд. ПРИНЯТ 24.09.2026.** Флаг `billboard?` переехал с текста и частиц
  в `node`: `trans()` берёт рыскание камеры сцены (`atan2` по третьей колонке её
  `world()`), у спрайта и меша флаг появился в `props()`. Аркада вместо `rot <= cam_rot`
  ставит `billboard true` и биндит камеру сцене (`cam <= Cam`), без этого узел камеры не
  видит.

- [x] **6.2. Режим линий.** `shape.mode()` получает `'lines'`, `draw` рисует `LINES`
  поверх батча при `wireframe?` панели. Возвращает чекбокс wireframe в аркаду и
  «Комнату». **Приёмка:** проба: с wireframe пиксель на ребре ящика белый.

- [x] **6.3. Туман. ПРИНЯТ 24.09.2026.** Юниформы `fog` (`vec2`) и `fog_color` (`vec3`)
  в `shader/solid` и его `plain`, затухание к цвету по расстоянию от камеры, ноль в конце
  значит «нет». Свойства `fog?` и `fog_color?` у панели, `fog_color` по умолчанию берёт
  цвет очистки. Чекбокс «Туман» в комнате, проба: дальняя стена темнеет на 59 единиц
  суммы, ближний пол на 4.

- [x] **6.4. Тайлинг плоскости по осям. ПРИНЯТ 24.09.2026.** `shape/plane.tile` это
  пара `(u, v)` `Float32Array`. Потребители (комната, «Ящики», аркада, шутер) считают
  пару по размерам карты, пол больше не растянут по глубине. Литералом из дерева пара не
  задаётся: список чисел ломает тип переопределения, нужен биндинг на метод.



- [x] **6.5 и 6.7. Тихая загрузка. ПРИНЯТ 24.09.2026.** `atlas.ready()` ложь, пока в
  слоях есть заглушка, `draw` читает только его и не бросает из `paint`.

---

# Движок общего назначения

Этапы 0–6 дают библиотеку для игр внутри приложений на $mol. Кирилл (23.09.2026):
нужен редактор и настоящая 3D-физика, цель это движок общего назначения. Ниже что
для этого не хватает, в порядке, в котором это имеет смысл делать. Размер каждой
задачи в днях одного молера с приёмкой, честно и с запасом.

Сейчас в движке 4 700 строк TS в 71 файле. Общее назначение это ещё примерно
25–30 тысяч строк, то есть 4–6 месяцев потоком из трёх-четырёх молеров. Это меньше,
чем «несколько месяцев с лютым геморроем» за счёт того, что UI, сборка, тесты,
синк и редактор `view.tree` уже есть в $mol, а не потому что задача маленькая.

## Этап 7. Редактор

Ставка та же, что у `bog/vmap`: сцена это `view.tree`, редактор правит исходник.
Редактор живёт отдельным паком `bog/gamestudio`, движок о нём не знает. Рабочее
имя, односложное.

- [x] **7.1. Инспектор узла. ПРИНЯТ 23.09.2026.** `bog/gamestudio/app`: три
  неподвижные `$mol_page`, дерево `$mol_list` по `scene.nodes()`, инспектор `$mol_form`
  с `$mol_number`, `$mol_select` по слоям атласа, чекбоксом; сцена-образец в
  `sample/`. Проба: колонки в ряд, холст рисует, клик показывает свойства. Коммит
  `c45b1f8` локально, репозиторий `b-on-g/gamestudio` ещё не создан. Грабля
  дев-сервера: `deploy` и `index.html` копируются в `-/` только по прямому запросу
  пути.
- [x] **7.2. Выбор мышью и гизмо. ПРИНЯТ 23.09.2026.** Клик через `point.pick`
  выбирает узел, стрелки X и Y и квадрат в батче поверх сцены (`doc.overlay`),
  размер постоянный на экране, перетаскивание пишет в узел каждый кадр и в документ
  один раз на `pointerup`. Проба: стрелка сдвинула монету на 80 px, ось держит, `y`
  не тронут. Хвосты: поворот и масштаб, перенос у узла с повёрнутым родителем,
  `pointercancel`, толщина линий 1 px. Коммит `2e8a415` локально.
- [x] **7.3. Сохранение в `view.tree`. ПРИНЯТ 23.09.2026.** `bog/gamestudio/doc`:
  `source` это текст `view.tree`, `set( узел, свойство, значение )` правит ровно одну
  строку, свой дамп по `span.row` (штатный `toString()` схлопывает списки из одного
  элемента), сцена интерпретируется из дерева через `props()` с мемо по тексту
  объявления. Четвёртая колонка «Исходник» с `$mol_textarea`. Проба: правка текста
  перекрашивает героя, правка в инспекторе даёт `pos / 5 0 0` без других изменений.
  Коммиты `29c298f`, `57ea9b5` локально.
- [x] **7.3а. Компиляция вместо интерпретации. ПРИНЯТ 23.09.2026.** Движок принимает
  `ArrayLike` в векторах, `kids?`, `tint` у узла (коммит `783ea4e`). Редактор компилирует
  документ штатной цепочкой `$mol_view_tree2_to_text` и `new Function`: 0.3 мс на сборку
  класса, бандл редактора +71 КБ, `unsafe-eval` в README. Диалект: `pos / 1 2 0` в одну
  строку раскладывается из цепочки в элементы перед компиляцией, литералы свойств узлов
  переписываются в `prop? <=> Node_prop?`, чтобы гизмо писал в узел штатно. Часы вне
  документа переживают пересборку. Коммит `7d5dde2` локально.
- [x] **7.3б. Пересборка сцены без миганий и утечек. ПРИНЯТ 23.09.2026.** Картинки
  кэшируются статическим мемо по uri с `$mol_wire_solid`, слоты и текстуры `draw`
  стали классами с `dispose`, пропавшие батчи и атласы освобождаются, деструктор
  панели чистит всё. Проба редактора: восемь пересборок, текстур 8/8, буферов 48/48,
  картинок загружено 0. Коммит `42443e3`.
- [x] **7.4. Ассеты. ПРИНЯТ 23.09.2026.** Вкладки «Сцена | Ассеты» в левой колонке, список
  из `assets` с превью, перетаскивание `$mol_drag`/`$mol_drop` и выбор плюс клик:
  картинка становится спрайтом, GLB мешем со своим батчем и загрузчиком по uri, звук
  строкой в `Sound uris`. `doc.add`, `doc.declare`, `doc.add_uri` одной вставкой текста.
  Хвост 7.4б: список ассетов из `meta.tree` сборщиком. Коммит `1b7f39a` локально.
- [x] **7.5. Тайлы. ПРИНЯТ 23.09.2026.** Вкладка «Тайлы» с палитрой из `palette`
  документа, инструменты «Клетка», «Прямоугольник», «Заливка», превью и запись в карту
  одной правкой, `Esc` снимает инструмент. Карта в документе живёт у подкласса сцены с
  `map` и `palette`, клетки рисует отдельный батч. Проба: клик кистью меняет ровно один
  символ, заливка десять, пиксель в точке клика цвета стены. Коммит `bf89add` локально.
- [x] **7.6. Играть в редакторе. ПРИНЯТ 23.09.2026.** Режимы правка/игра/пауза,
  снимок холодного состояния через `props()` и откат на «Стоп», гизмо и поля
  инспектора выключены в игре, герой образца ходит по WASD через `input` сцены.
  Проба: 5.00 → 6.50 за 30 кадров и обратно к 5.00, исходник не тронут. Хвосты:
  откат созданных в игре узлов, горячая подмена кода из vmap S2. Коммит `1947a89`.
- [x] **7.6а. Путь от узла к сцене. ПРИНЯТ 23.09.2026.** `kids?` ставит `parent`,
  `root()`, `is_scene()`, `scene()`, `input()`, `clock()` у узла; обход сцены проставляет
  родителей узлам из дерева; ходок берёт ввод сцены сам. Коммит `91fc663`.
- [x] **7.7. Совместная правка. ПРИНЯТ 23.09.2026.** Текст сцены одним атомом ленда,
  запись отложена тиком, до подтверждения показывается локальный черновик, чтобы
  редактор не мигал своим же текстом. Присутствие словарём по id: имя, выбор, точка
  указателя, метка; чужие курсоры стрелкой с подписью, чужой выбор красит строку
  дерева. Задержка правки между редакторами 83 мс при пороге 500. Сеет образец только
  создатель ленда, иначе второй редактор затёр бы документ пустотой. Слияния текста
  нет, побеждает последний писатель. Коммит `b94231f` локально.

Итого около 45 дней. Редактор без 6.2 (линии) и без 7.3 бесполезен, поэтому порядок
такой: 6.2, 7.1, 7.3, 7.2, остальное параллельно.

## Этап 8. Настоящая 3D-физика

Два пути. Свой солвер это 4–5 тысяч строк и полтора месяца одного сильного молера,
но всё на одном языке, тестируется в ноде и живёт в горячей зоне без копирования
данных. Rapier через wasm это готовое и быстрое, но 2 МБ бандла, второй язык в
проекте, копирование состояния туда-обратно каждый кадр и чужие правила. Ставка:
**свой, по шагам, с Rapier как контрольным замером** в спайке.

- [x] **8.0. Спайк. ПРИНЯТ 23.09.2026.** Rapier через `@dimforge/rapier3d-compat`, 1 000
  ящиков на пол, M4 Pro, Metal, три прогона (отчёт в истории, коммит `766aa6a`):

  | тел | шаг Rapier | копирование в атомы | главный поток |
  |---|---|---|---|
  | 200 | 0.5–0.7 мс | 0.6 мс | 2.1–2.8 мс |
  | 1 000 | 1.0–1.8 мс | 1.8–2.4 мс | 5.3–7.5 мс |
  | 3 000 | 4.6–4.9 мс | 6.8–7.9 мс | 20.6–28.1 мс |

  **Планка своему солверу: тысяча ящиков за шаг не дольше 5 мс.** Второй вывод
  важнее: на трёх тысячах физика это 5 мс, а атомы на ящик (копирование плюс
  `world()` в `batch.fill()`) это 20 мс. Тела 3D-физики живут в горячей зоне и
  рисуются из её буферов напрямую, без узла на тело (правка 8.1 и 8.7). Бандл с
  Rapier тяжелее на 2.75 МиБ, и MAM не тянет пакеты с кодом в `dist/*.cjs`,
  библиотеку пришлось вендорить и переписывать в IIFE. Спайк выброшен.
- [x] **8.1. Тела и формы.** `phys3/body`: масса, инерция, `pos`, `rot` кватернионом
  (нужен `quat` в `vec`), `vel`, `ang`. Формы: сфера, ящик, капсула, выпуклая
  оболочка из точек, плоскость. Всё в плоских буферах, тело это индекс; узел на тело
  только у именованных (герой, дверь), масса ящиков без узлов (вывод 8.0). 6 дней.
- [x] **8.2. Широкая фаза.** Sort-and-sweep по одной оси или динамическая сетка
  клеток; пары кандидатов без аллокаций. 4 дня.
- [x] **8.3. Узкая фаза.** Сфера×сфера, сфера×ящик, ящик×ящик через SAT, общий
  случай выпуклых через GJK и EPA, контакт с нормалью и глубиной, манифолд до
  четырёх точек. Самая трудная часть, тесты на каждую пару. 12 дней.
- [x] **8.4. Солвер. ПРИНЯТ 23.09.2026.** Последовательные импульсы с трением,
  упругостью, Baumgarte, warm starting по хешу без аллокаций, сон. Тысяча ящиков в
  куче над плоскостью: средний шаг 3.45 мс (broad 0.36, narrow 0.38, solve 2.79 при
  4 200 контактах), максимум 5.8 мс в момент удара кучи. Планка 8.0 выдержана.
  Известное: контакт только при глубине ≤ 0, без спекулятивного запаса, тело на
  нулевой глубине один кадр «подпрыгивает» на 3e-5 и садится на `slop`. Коммит
  `20b1362`.
- [x] **8.5. Персонаж и луч. ПРИНЯТ 23.09.2026.** `phys3/cast`: луч и sweep по всем
  формам, оболочка через conservative advancement по GJK, 5 мкс на луч против тысячи
  ящиков; симплекс GJK в Float64, на float32 не сходился. `phys3/walker`: капсула,
  гравитация, прыжок, скольжение, ступеньки до `step_height`, склоны до `slope`,
  прилипание к полу на спуске, 2–6 мкс на шаг. Коммит `d804a1e`.
- [x] **8.6. Шарниры. ПРИНЯТ 23.09.2026.** Точечный (блок 3×3), петля с пределами,
  ползун, пружина; вплетены в итерации контактного солвера, warm starting по индексу,
  спекулятивные пределы без перелёта. 250 шарниров на тысяче ящиков стоят 0.35 мс.
  На странице «Ящики» кнопки «Цепь» и «Дверь». Тела одного шарнира друг с другом
  сталкиваются, фильтра пар нет, звенья цепи взяты с зазором. Коммит `a0af11a`.
- [x] **8.7. Сцена и редактор. ПРИНЯТ 23.09.2026.** Страница «Ящики»: мир `phys3` в
  сцене, батч читает `trans` из буферов мира (`source` со `skip` плоскости), контакты
  линиями через узел `phys3/debug` и живой буфер линий в `draw`, клик бросает ящик как
  именованное тело-узел. Проба: 301 тело, контакты есть, ни один ящик не ниже пола,
  клик добавляет тело. Коммит `a344dfb`.
- [x] **8.8. Замер против 8.0. ПРИНЯТ 23.09.2026.** В браузере на Metal, страница
  «Ящики», тысяча тел: свой солвер 3.7 мс на шаг при 60 fps и 3 500 контактах, Rapier
  давал 1.0–1.8 мс. Отставание в 2–3.7 раза, в пределах «не больше чем втрое» по
  медиане, без wasm и без 2.75 МиБ. Дальше ускорять только по профилю: солвер это
  80 % шага.

Итого около 50 дней. Плоская `phys` остаётся для 2D-игр, ей 3D-солвер не нужен.

## Этап 9. Рендер общего назначения

- [x] **9.1. Материалы и свет. ПРИНЯТ 23.09.2026.** Узлы `light` (sun, point, spot),
  до восьми в сцене юниформ-массивами одной заливкой, Cook–Torrance с GGX, Smith и
  Fresnel, `material` vec4 на инстанс (metallic, roughness, emissive), нормал-мапы через
  производные в фрагменте, слой в атласе. Комната: два точечных, прожектор на ходоке,
  чекбокс «Блики». Проба: тёплый пол краснее холодного, с бликами максимум строки 765
  против 401. Коммит `9863b72`.
- [x] **9.2. Тени. ПРИНЯТ 23.09.2026.** Карта глубины 2048² для солнца на сферу радиусом
  20 вокруг камеры, программа глубины с тем же порядком входов (явные `layout( location )`,
  один VAO на оба прохода), PCF 3×3, флаги `shadows?`, `shadow_size?`, `shadow_range?`.
  Проба: пол в тени 129 против 247 без теней, открытый пол не изменился. Цена около 3 мс
  на M4 Pro вместе с выборками PCF. Хвосты: каскадов нет; тени отбрасывают только узлы,
  прошедшие пирамиду камеры, поэтому при развороте от кучи тени пропадают (нужна отдельная
  заливка под пирамиду света). Коммит `629cbec`.
- [x] **9.3. Скелет и анимация. ПРИНЯТ 23.09.2026.** Разбор скинов, скелета и клипов из
  GLB (LINEAR и STEP), `skin` держит позу до 64 костей в одном буфере и смешивает два
  клипа, матрицы костей едут в шейдер текстурой `RGBA32F`, `shader/skin` наследует
  солид и добавляет суставы с весами, `mesh/skin` узлом. Тестовая модель с двумя
  костями и двумя клипами в `skin/model/arm.glb`. Коммит `b2077e8`.
- [x] **9.3а. Скелет подключён. ПРИНЯТ 24.09.2026.** Юниформ костей и атрибуты суставов
  в панели, текстура костей на своём юните, рука из GLB в комнате по чекбоксу.
- [x] **9.4. Частицы. ПРИНЯТ 23.09.2026.** Эмиттер держит частицы в плоских буферах и
  отдаёт себя источником батча (`trans`, `tint`, `layer`, `uv`, `aabb`), рождение по
  накопителю, `burst`, билборд к камере сцены, цвет и размер по возрасту. Вспышка при
  подборе монеты на «Плоском мире».
- [x] **9.5. Постпроцессинг. ПРИНЯТ 23.09.2026.** Кадр в текстуру `RGBA16F`, цепочка
  полноэкранных проходов из `gl_VertexID` без атрибутов: ACES, блум в четыре шага через
  половинное разрешение, виньетка; цели пинг-понгом, пересоздание по размеру холста,
  освобождение в деструкторе. Сэмплеры разведены по юнитам: атлас 0, тени 1, вход поста
  2, блум 3. На GPU кадр не меняется, на SwiftShader блум стоит 7 мс. Коммит `228db60`.
- [x] **9.6. Отсечение и LOD. ПРИНЯТ 23.09.2026.** Пирамида камеры по Гриббу–Хартманну
  в сцене, `batch.fill` отсекает узлы по сфере и инстансы источника по `aabb` с
  уплотнением, диапазоны `near`/`far` на батч как LOD, `lods` у меша только API.
  «Ящики»: `drawn 295 / 300` лицом к куче, `0 / 300` спиной. Хвост: сортировка
  прозрачных в `draw`; `aspect` сцены пишет `draw` (строка ждёт освобождения файла).
  Коммит `a2d1ab7`.
- [x] **9.7. Текст в мире. ПРИНЯТ 23.09.2026.** Атлас принимает готовые картинки
  (`sources`), шрифт рисует глифы на канвасе и отдаёт их слоями с таблицей ширин,
  `text` собирает строку в пул инстансов и отдаёт его источником батча, с
  выравниванием и билбордом. Над монетами «Плоского мира» надписи «+1». В ноде канваса
  нет, шрифт это видит и отдаёт пустые глифы. Коммит `8e3db60`.
- [ ] **9.8. WebGPU.** Второй бэкенд в `gl/` за тем же интерфейсом, compute для
  частиц и скиннинга. Когда WebGL2 упрётся, не раньше. 15 дней.

Итого около 60 дней без WebGPU.

- [x] **9.10. Руководство «с чего начать». ПРИНЯТ 25.09.2026.** `START.md`: четыре шага
  от пустого пака до едущего спрайта, каждый собран и проверен на живом дев-сервере во
  временном паке, аудит проходил на каждом шаге отдельно, а не только в конце. Проверка
  движения написана командой, которую можно выполнить в ноде, без рендера. Дальше по
  разделу на карту и столкновения, атлас и кадры, ввод, камеру, интерфейс поверх холста,
  звук; каждый кусок кода взят из трёх игр или демок с указанием файла-источника, цитаты
  сверены грепом. Грабли, которые новичок поймает в первый час (холст не корень страницы,
  односторонний `pos <=` глотает запись из шага, `$mol_scroll` в `$mol_page` схлопывает
  холст, картинки атласа одного размера, состояние ввода полями), вписаны прямо в шаги со
  ссылками на архитектуру, а не в конец списком.
  Попутно выяснилось, что **раздел «Чего у нас нет» в COMPARE.md врал в свою сторону**:
  перечислял как отсутствующие навигацию, поведение, сохранения, сеть, тени, скелетную
  анимацию, частицы и постпроцессинг, то есть восемь принятых этапов. Переписан.

## Этап 10. Игровые системы

- [x] **10.1. Префабы. ПРИНЯТ 25.09.2026.** Узел-шаблон в `view.tree`, инстансы с
  переопределением свойств. Движку не понадобилось ни строки, вся работа вышла в
  редакторе: четыре поломки адресации плюс две, найденные по дороге (`chain( name )`
  собирает свойства по цепочке баз, `own( klass, line )` решает, своя ли строка, иначе
  правка чужой строки молча терялась). Инспектор подписывает «часть префаба, затронет N»,
  кнопка «Переопределить в этом инстансе» заводит верхнеуровневое объявление и меняет
  строку на ссылку. Запрещённая вложенная форма проверена дословно: даёт
  `Unexpected token 'const'`, тест ловит это и по структуре, и по компиляции.
  Сама кнопка завела дыру, которую пришлось закрывать тем же коммитом: `refs` не видел
  строку-подмену, поэтому после переопределения инспектор продолжал адресовать общее тело
  префаба, то есть действие чинило документ и тут же ломало адресацию.
  **Долг на будущее.** Подпись считает по имени объявления, а не по свойству: в нынешней
  модели «своё значение» и «своё объявление» — одно и то же, ветка по свойству оказалась
  недостижимой и выкинута вместе с параметром. Настоящей она станет при правке тела
  префаба напрямую, куда инспектор пока не попадает (`nodes()` перечисляет только
  развёрнутые инстансы). Тот, кто заведёт выбор префаба, обязан вернуть вторую ветку
  вместе с тестом, который на ней падает.
  Движковая половина закрыта 25.09 **без единой строки кода.** Модуль `prefab` был
  написан (шаблон-объект, ячейка на свойство на инстанс, клонирование детей) и удалён
  коммитом `324a81d`: наследование классов даёт то же самое статически и дешевле,
  `new $my_guard` закрывает и спавн в рантайме, а живая правка шаблона без пересборки
  нужна только при запущенной сцене, где это правка правил игры на ходу.
  **Заложенная мина в выбранной форме документа.** Префаб это второе верхнеуровневое
  объявление в том же файле, а класс теряет рукописное тело базы, если база объявлена
  в том же `.view.tree`: топосорт кладёт сгенерированный `.js` перед `.view.ts`, и
  гранулярность там файловая, а не классовая. Сейчас не стреляет, потому что редактор
  компилирует документ в рантайме и рукописного тела нет вовсе. Выстрелит в день, когда
  документ с префабом соберут статическим модулем.
  Разведка 25.09 на живом бандле: **движку править нечего, префабы уже работают.**
  Два инстанса одного класса развернулись со своими под-узлами, переопределённые
  свойства компилируются в отдельные мемо на инстанс, запись в один не задевает другой.
  Вся работа в редакторе, и там четыре поломки, все воспроизведены: корень документа
  кладётся под пустым ключом для каждого верхнеуровневого объявления и побеждает
  последнее (то есть корнем становится база префаба); из-за этого обход документа
  возвращает узел из тела префаба вместо инстансов сцены; сбор ссылок берёт `kids /`
  только из собственного тела, а у инстанса его нет; `unlock` не трогает верхнеуровневые
  классы, поэтому запись унаследованного свойства глотается молча.
  **Принятые решения по интерфейсу.** Префаб — второе верхнеуровневое объявление в том
  же файле документа, отдельный модуль отпадает: редактор обязан видеть унаследованное
  тело как дерево. Новых базовых классов нет, префаб это любой узловой класс. `props()`
  движок не расширяет: в рантайме разницы между унаследованным и переопределённым
  свойством нет, признак происхождения считает редактор по дереву.
  **Общий под-узел.** Правка свойства под-узла инстанса по умолчанию пишется в тело
  префаба, инспектор подписывает «часть префаба, затронет N инстансов». Переопределение
  под-узла на конкретном инстансе редактор пишет **только корневым портом плюс ссылкой**,
  вложенным блоком — никогда: редекларация под-узла стирает все родительские биндинги, а
  значение-класс внутри блока экземпляра компилируется в невалидный JS (репро
  `$doc $mol_view Box $mol_scroll Sub $mol_view title \x`). Скалярное переопределение на
  инстансе вложенным блоком писать можно, оно компилируется верно.
- [x] **10.2. Навигация. ПРИНЯТ 23.09.2026.** `nav/grid` A* с бинарной кучей без
  аллокаций и сглаживанием строкой видимости (0.29 мс на лабиринте 100×100),
  `nav/mesh` navmesh с порталами и funnel (`from_tile` жадными прямоугольниками,
  0.05 мс на пути через 200 многоугольников), `nav/agent` с перепланированием и
  отталкиванием. На «Плоском мире» призрак догоняет героя по сетке. Хвост:
  триангуляция произвольного пола. Коммит `735b498`.
- [x] **10.3. Поведение. ПРИНЯТ 23.09.2026.** `brain/fsm` (состояния как узлы, переходы
  данными `to`/`when`, условия по `props()` владельца, `state` атомом для инспектора),
  `brain/bt` (`seq`, `sel`, `par`, `inv`, `wait`, `cond`, `act`), пример `act/move`.
  Логика состояний это `tick`, не `step`, чтобы сцена не шагала их сама. Коммит `b4a07c1`.
- [x] **10.4. Сохранение. ПРИНЯТ 23.09.2026.** `save/save.ts`: `dump( scene )` по
  `props()` с `klass` и временем часов, `load( scene, data )` по титулам, `slot( name )`
  на `$mol_state_local`, сеттер `clock.time`. Хвост: `save/land` в Гипер Базе (путь
  описан: один атом с JSON, аксессор без мемо, запись из фибры). Коммит `05b26a0`.
- [x] **10.5. Сеть по-настоящему. ПРИНЯТ 23.09.2026.** `net/`: комната это ленд, игрок
  сущность с позицией одним атомом, `self` шлёт своё тело не чаще `rate`, `other`
  интерполирует чужих с запаздыванием в интервал, присутствие по метке, лобби списком
  комнат, стенд `net/stand`. Проба двумя браузерами с локальным мастером: 34–38 мс при
  пороге 200 (первый обмен 280–540 мс, это холодная подписка, идёт прогревом).
  Коммит `fb60641`.
- [x] **10.6. Звук в пространстве. ПРИНЯТ 23.09.2026.** `play( name, pos )` через
  панораму и затухание по слушателю, обновление голосов 10 Гц цепочкой таймеров,
  `music( name )` с кроссфейдом, общие `effects` и `volume`. Настройки биндить из
  дерева, императивная запись в мемо без читателя сбрасывается. Коммит `fab4e14`.
- [x] **6.10. Сэмплы без держателя. ПРИНЯТ 23.09.2026.** Комната и сэмплы держатся
  между проигрываниями, буфер не перекачивается.
- [x] **10.7. Экранное управление. ПРИНЯТ 23.09.2026.** `input/screen`: джойстик и
  кнопки как вид, состояние полями, третий источник для `input`; чекбокс «Кнопки» на
  страницах «Плоский мир» и «Комната», проба водит джойстиком.

Итого около 35 дней.

## Этап 11. Конвейер и платформы

- [x] **11.1. Упаковка ассетов. ПРИНЯТ 23.09.2026.** `pack/pack.node.ts`: скан папки,
  квадраты через одну страницу headless Chrome, `atlas.meta.tree` с `deploy` и
  `atlas.json` со слоями, CLI `node bog/gamengine/pack/-/node.js <dir> <out> [size]`.
  Хвосты: KTX2/Basis, Draco, nearest для пиксель-арта. Коммит `15d4558`.
- [x] **11.2. Десктоп и мобильные. ПРИНЯТ 23.09.2026.** `screen/`: полный экран и
  захват курсора мемо-флагами с побочками через тик, `take( out )` для мыши; офлайн
  через `$mol_offline`, манифест PWA с иконками, политика обновления и раздел про
  Tauri в README. Проба открывает страницу по IP, воркер там не ставится. Коммит
  `c708a10`.
- [x] **11.2а. Мышь и полный экран в комнате. ПРИНЯТ 25.09.2026.** У ходока `screen`,
  `sense` (0.003 радиана на пиксель) и поле-буфер на два числа под `take()`. Тангаж
  упирается в `π/2 - 1e-3`, чтобы взгляд не переворачивался через темя. Клавиши и мышь
  копят углы в локальных переменных и пишут `rot` одним новым буфером за шаг, аллокаций
  в горячем пути не прибавилось. В комнате «Во весь экран» сделан чекбоксом, а не
  кнопкой: браузер сам гасит полный экран по Esc, и двусторонний чекбокс это показывает.
  Захват курсора просится из обработчика `pointerdown`, то есть из пользовательского
  жеста. Узел экрана назван `Screen_api`, потому что `Screen` в этом дереве уже занят
  экранными кнопками.
- [x] **11.3. Профилировщик. ПРИНЯТ 23.09.2026.** `draw.report()`: тик, заливка, тени,
  проход, пост, батчи, инстансы, вызовы, треугольники, байты; кольца по 30 кадров, в
  `paint` без аллокаций. Чекбокс «Профиль» в комнате показывает таблицу в подвале.
  Коммит `4ebda38`.
- [x] **11.4. Игры-образцы. ПРИНЯТЫ 23–24.09.2026.** Платформер `bog/jumper` (867 строк),
  шутер `bog/shooter` (1259), стратегия `bog/legion` (1259). У каждой своя проба и CI.
  Стратегия держит сто юнитов с навигацией и машинами состояний за 3.79 мс на кадр при
  911 холодных узлах: запас к потолку холодной зоны примерно вдесятеро.

Итого около 25 дней.

## Порядок

Первыми идут вещи, без которых остальное не проверить: режим линий (6.2), инспектор
и сохранение (7.1, 7.3), спайк Rapier (8.0) как планка. Дальше две ветки
параллельно: редактор и физика, они не пересекаются по файлам. Рендер (9) после
того, как редактор умеет показать результат, иначе свет и тени нечем настраивать.
Системы (10) по мере появления игр-образцов из 11.4, которые их и потребуют.

- [x] **6.8. Аудит аркады.** `bog/game/arcade` собирается и проба зелёная; аудит на
  23.09 вечером чистый, но на CI у `bog/game` workflow нет, и раньше он ловил три
  ошибки строгости: `place_by_pos` нет в `$bog_game_realm`
  (`actor.view.tree` пробрасывает свойство, которого нет в классе), `wall_frame`
  через `keyof ReturnType< this['place_skins'] >` и следом TS2417. Поправить в
  `bog/game`, добавить workflow с пробой по образцу движка. 1 день.

- [x] **6.9. Меню каталога на широком окне. ПРИНЯТ 25.09.2026.** Причина не в книге:
  холст отдаёт `width`/`height` в физических пикселях, и при `dpr 2` страница, которая
  считается по содержимому, хочет вдвое больше места и съедает окно. Подробно граблей
  3.15 в архитектуре. Правка в двух файлах демо, ни строки в `mol/` и ни строки в `draw/`:
  у демо убраны распорки (`placeholders /$mol_view`), страницам даны `flex: 1 1 0` и
  `minWidth: 0` под `@media (min-width: 60rem)`. Медиазапрос нужен: без него на окне
  400 px холсту доставалось 227, потому что меню навсегда занимало свои 149.
  Загадочные 640 объяснились там же: распорка каталога это такой же `flex: 1 1 0`, и
  свободное место делилось с ней поровну.
  **Почему разведка 24.09 провалилась:** проба зашивает `deviceScaleFactor: 1`, поэтому
  на ней бага нет вовсе, и любая правка выглядела бесполезной. Сценарий пробы на меню
  добавлен шестым, ширина 1440.

- [x] **6.11. Батчи по фигуре узла. ПРИНЯТ 23.09.2026.** `scene.auto_batches()`
  группирует узлы по тройке шейдер×фигура×атлас, шейдер берётся из `node.shader()` или
  по классу (меш, меш без атласа, спрайт), фигура из `node.shape()` или общий квад;
  явные `batches` в дереве перекрывают. Критерий «узел рисуется» это наличие `atlas()`,
  `layer()` и `uv()`, поэтому эмиттеры и текст, кормящие батч источником, в автобатчи не
  попадают. `shader/solid/plain` для мешей без текстуры. Коммит `765ccd2`.
- [x] **7.8. Инспектор для списков записей. ПРИНЯТ 23.09.2026.** `kind: 'list'` с
  описанием полей у свойства узла, строки с добавлением и удалением в инспекторе,
  применено к переходам машины состояний.

- [x] **6.12. Цвет очистки. ПРИНЯТ 24.09.2026.** `draw.clear?` vec4 с прежним дефолтом.


- [x] **6.13. Тайлмап одним узлом. ПРИНЯТ 23.09.2026.** `tilemap` держит пул инстансов
  по карте и палитре и отдаёт его источником батча; `tile.cell_pos` и `tile.cell_at`
  убрали третью копию формулы. «Плоский мир»: было 314 узлов сцены, стало 14.
  Коммит `43a020e`.
- [x] **6.14. Шов для генерируемых узлов сцены. ПРИНЯТ 23.09.2026.** `scene.auto_nodes()`
  складывается с `kids()` в `nodes()`, родители проставляются, автобатчи их видят.
  Коммит `77cca9a`.

- [x] **6.15. Источник батча без столкновения имён. ЗАКРЫТ 24.09.2026 без правок.**
  Столкновение имён осталось: поле `trans` в наследнике узла это TS2416, проверено
  пробным полем в `text`. Но после 13.6 узлу и не нужно быть источником самому: он
  объявляет себя парой `is_source()` и `source()`, а буферы держит отдельный пул, и это
  ровно граница холодного и горячего из раздела 1 архитектуры. Обёрток, которые
  переименование убрало бы, не осталось: последнюю, ручной `emit()` в редакторе, снял
  пункт 6.16. Переименование `trans` в `inst_trans` тронуло бы `batch`, `text`,
  `particle`, `tilemap`, `phys3/debug` и деревья трёх приложений ради возможности,
  которой никто не пользуется.

- [x] **7.9. Редактор без ручных батчей. ПРИНЯТ 23.09.2026.** Документ больше не пишет
  батч на каждый меш, сцена собирает их сама; модель рисуется своей фигурой. Ручным
  остался только батч клеток тайловой карты: они не входят в `nodes()`, иначе дерево
  сцены показывает три десятка клеток и клик по полу выбирает клетку.

## Этап 12. Уроки первой игры

Платформер `bog/jumper` (11.4, принят 23.09.2026) написан на движке за один заход и
дал список того, что каждая следующая игра будет переписывать заново. Пункты идут по
убыванию боли.

- [x] **12.1. Гравитация и стороны удара. ПРИНЯТ 23.09.2026.** `phys.gravity?` для
  подвижных не-призраков, битовая маска касаний за шаг (`on_ground`, `on_ceil`,
  `on_wall`) и нормаль вторым аргументом `hit`. Нормаль указывает туда, куда тело
  вытолкнули: приземление даёт `(0, 1)`. Бит ставится на любую заблокированную ось, а
  не только при реальной поправке, иначе лежащее тело земли не видит. Игра потеряла
  щуп земли и перестала дрожать на полу. Коммит `2cba7cc`.
- [x] **12.2. Опрос мира для ИИ. ПРИНЯТ 23.09.2026.** `tile.char`, `tile.ahead`,
  `tile.edge` без аллокаций.
- [x] **12.3. Условия машины мимо `props()`. ПРИНЯТ 23.09.2026.** `cond( name )` сначала
  ищет метод владельца, потом свойство; игра убрала признаки с пустыми сеттерами из
  инспектора.
- [x] **12.4. Состояние знает хозяина. ПРИНЯТ 23.09.2026.** `state.owner()` через машину;
  порядок шага решён флагом `is_brain()` у узла: сцена кладёт детей-мозгов перед
  родителем, поэтому переход виден владельцу в том же кадре.
- [x] **12.5. Камера-следопыт. ПРИНЯТ 23.09.2026.** `cam_flat.target`, `bounds`,
  `follow`; сцена дошагивает свою камеру, если её нет среди узлов. Игра выбросила
  ручной кламп по краям уровня.
- [x] **12.6. Карта разбирается дважды. ПРИНЯТ 23.09.2026.** `tile.spots( char )` и
  `tile.chars()`; игровой разбор уровня свёлся к соответствию символа кадру и точке
  старта.

- [x] **12.7. Неприжившаяся запись позиции падает. ПРИНЯТ 23.09.2026.** `move` читает
  `pos` обратно и бросает с именем узла, если дерево сделало свойство геттером.

- [x] **6.16. Текст без ручного `emit`. ПРИНЯТ 24.09.2026.** `pool()` стал мемо, которое
  зовёт `emit()` при чтении, буфер лежит в `pool_own()`. Свежесть считается по готовым
  осям, поэтому билборд больше не пересобирает строку каждый кадр, а пул держит `version`,
  которая растёт только при настоящей пересборке. `step()` у текста больше нет, редактор
  отдаёт `Mate_text.pool()` батчу напрямую.

- [x] **6.17. Свойства физики под смётом. ПРИНЯТ 24.09.2026.** Выбрано «сцена трогает
  явно»: у `phys` и `phys3` есть `pull()`, который читает свои входы, `scene.step()` зовёт
  его сразу после чтения `phys`/`phys3`, до гейта. Полями с сеттером не стали, чтобы не
  терять биндинги из дерева. Тест «gravity of phys set by code lives through two frames»,
  негативный прогон падает `[0,0]` против `[0,-10]`. Попутно: `scene.batches([])` на
  пустой сцене ничего не инвалидирует, поэтому три старых теста «пересчёта в том же
  кадре» ничего не проверяли, им заменили толчок на `scene.aspect( 2 )`.

- [x] **9.9. Цветовой конвейер. ПРИНЯТ 25.09.2026.** У атласа появилось назначение:
  цветовой заливается в `SRGB8_ALPHA8`, атлас данных в `RGBA8`, и цветовой ссылается на
  парный через `data()`. Батч, группа и слот отрисовки по-прежнему знают ровно один
  атлас, поэтому ни батчи, ни сцену править не пришлось. Нормал-мапа берётся из атласа
  данных, а не ищется в общем. Гамма встала в конец тон-маппинга вместо нормировки по
  белому, комната перестала быть пересвеченной. Побочное: 1.0 больше не белый, каркас
  даёт 230 вместо 254.
  Важнее самой задачи оказалась грабля 3.14: авторские константы цвета были написаны
  глазами как экранные, и в линейном конвейере поехали. Цвет очистки, цвет тумана и порог
  свечения возведены в 2.2. При старых числах туман подсвечивал дальнюю стену вместо
  затемнения, а свечение держалось на запасе в 18 единиц вместо 111.
  Из проб поехала ровно одна проверка из двадцати одной. Ослаблять её порог не стали:
  отношения яркостей теперь сравниваются в линейном свете, где порог 1.3 держится и на
  новом конвейере (1.72), и на старом (2.26). Разности остались в экранном, там у них
  запас больше.

- [x] **7.10. Вложенные узлы в инспекторе. ПРИНЯТ 25.09.2026.** Узел документа
  адресуется путём из имён объявлений от корня через `/` (`Guard/Walk`), а не индексом
  строки. `doc.nodes()` спускается вглубь, `path_at( steps )` переводит путь индексов в
  путь имён и отдаёт пустую строку на неразрешимом шаге, поэтому авто-узлы сцены
  по-прежнему молча не пишутся в документ. Поиск по заголовку убран совсем: он был
  второй половиной той же грабли, во вложенном дереве заголовки не уникальны.
  В `unlock` появился разбор `bound( tree )`: строка с `<=`, `<=>` или `=>` больше не
  оборачивается в неизменяемый литерал, а обходится рекурсивно. Без этого `kids /`
  вложенного узла схлопывался в константы, что ломало не только гизмо, но и снимок для
  запуска и остановки сцены, он пишет через тот же набор свойств.
- [x] **7.4б. Список ассетов из мета-файлов. ПРИНЯТ 23.09.2026.** `assets.json` делает
  серверный `pack`, редактор читает его с запасным списком из дерева. `deploy` кладёт
  файл по полному пути от корня MAM, а не плоско в `-/`.

## Этап 13. Уроки второй игры

Шутер `bog/shooter` (11.4, принят 23.09.2026, 1259 строк без тестов и генератора
ассетов) вскрыл то, чего не видел платформер: всё про объёмную физику как API.

- [x] **13.1. Исключения в луче и sweep. ПРИНЯТ 24.09.2026.**
- [x] **13.2. Ходок виден лучам. ПРИНЯТ 24.09.2026.** Тело-капсула в мире, кинематика,
  свой индекс в фильтре; размер капсулы пишется до постановки, иначе не шагавший ходок
  стоял точкой нулевого радиуса и был невидим.
- [x] **13.3. Стабильный хендл тела. ПРИНЯТ 24.09.2026.** Хендл переживает перестановку
  после удаления, узел помнит свой мир.
- [x] **13.4. Кинематическое тело. ПРИНЯТ 24.09.2026.** `move` обновляет границы и
  матрицу сразу.
- [x] **13.5. Состояние тела методом. ПРИНЯТ 24.09.2026.** `mass_set` и `ghost_set` у
  узла, свойства остаются для начальной настройки из дерева.
- [x] **13.6 и 13.12. Источники и линии. ПРИНЯТ 24.09.2026.** Узел объявляет себя
  источником парой `is_source()` и `source()`, автобатчи заводят ему батч без
  требования `uv()`; порядок батчей теперь порядок узлов, иначе тайлмап рисовался
  поверх героя. `batch.instances` рисует инстансы без узлов, у линий появился `count`.
- [x] **13.7. Тест `screen` починен. ПРИНЯТ 24.09.2026.** Заглушка получила метод
  захвата, шутер перестал прятаться за структурным типом.
- [x] **13.8. Замер шага в движке. ПРИНЯТ 24.09.2026.** `step_ms()` у обеих физик, свои
  буферы потеряли и демо, и шутер.
- [x] **13.9. Камера от первого лица. ПРИНЯТ 24.09.2026.** `cam_deep.follow` и `lift`,
  шутер собирает камеру одной строкой вместо трёх мемо.
- [x] **13.10. Поворот ходока полем. ПРИНЯТ 24.09.2026.**
- [x] **13.11. Разбор карты для объёма. ПРИНЯТ 25.09.2026.** Модуль `map` это чистый
  разбор текста без GL и без узлов: `spots( char )`, `chars()`, `ids( char )`, `xy`,
  `pos`, `place`, `spot_pos`, `center`, буферы наружу через `out`. Объём решает свойство
  `plane`: `xy` даёт `( x+0.5, -y-0.5, lift )`, то есть ровно прежний `cell_pos`, а `xz`
  даёт `( x+0.5, lift, y+0.5 )` — точки для стен, столбов и появления. `phys_tile`
  наследует разбор и держит только проходимость, 104 строки против 46. Арена шутера
  похудела со 110 строк до 56, в ней осталось только игровое. Побочное изменение:
  `rows()` теперь массив строк, а не массив массивов символов.
- [x] **13.11а. Комната на общем разборе карты. ПРИНЯТ 25.09.2026.** Ручной обход строк
  и разбор идентификатора убраны, стены берутся из общего разбора при `plane 'xz'`.

## Этап 14. Уроки третьей игры

Стратегия `bog/legion` (100 юнитов, 911 холодных узлов, 3.79 мс на кадр) показала, чего
не хватает играм с массой объектов и свободной камерой.

- [x] **14.1. Свободная камера. ПРИНЯТ 25.09.2026.** У `cam_flat` общий кламп `place( x, y )`
  с учётом высоты, зума и соотношения сторон, им пользуются и слежение, и `pan( dx, dy )`.
  `zoom_at( factor, x, y )` держит точку на месте, зажим в `zoom_min`/`zoom_max`.
  Точка мировая, а не экранная: камера про пиксели холста не знает, перевод делает
  потребитель через `point.world()`. Прокрутка краями вынесена в узел `cam/edge`, он
  кладётся в `kids` сцены и шагает сам. Своей камеры в стратегии больше нет.
- [x] **14.2. Цель агента полем. ПРИНЯТ 24.09.2026.** `aim( x, y )` и `stop()` поверх
  полей, `target` остался входом из дерева обычным методом.
- [x] **14.3. Прямоугольный выбор. ПРИНЯТ 24.09.2026.** `point.pick_box` с буфером-полем,
  отдаёт узлы и, по желанию, их индексы.
- [x] **14.4. Плотность пикселей. ПРИНЯТ 24.09.2026.** `draw.dpr()` свойством (метода в
  `.view.ts` мало: потребитель типизируется по `.d.ts` дерева) и `point.scale?`, один
  множитель на все переводы координат. Стратегия стала короче на 23 строки.
- [x] **14.5. Групповая цель. ПРИНЯТ 25.09.2026.** `nav/squad.order( nodes, x, y, grid )`
  раскладывает квадрат вокруг точки шагом `radius * 2.4`, занятый или упёршийся в стену
  слот ищет спиралью по кольцам и ставит каждому агенту `aim`. Буферы полями, после
  первого вызова аллокаций нет.
- [x] **14.6. Примитив боя. ПРИНЯТ 24.09.2026.** Бой это компонент (`$mol_object2` с
  `owner`), а не узел: владелец вешает его свойством, как мозги и скин. Здоровье, урон,
  перезарядка и оживление ушли из юнита стратегии. Владение через обычный метод, не
  мемо: взаимное клеймение владельца и компонента даёт бесконечную рекурсию при первом
  обращении к контексту (в дереве не всплывает, на голых `new` в тесте падает сразу).
- [x] **14.7. Снятие узла со сцены без пересборки. ПРИНЯТ 25.09.2026.** У узла поле
  `hidden` и `shown()`, который идёт вверх по родителям, поэтому скрытый узел уносит и
  детей. Шаг сцены и `batch.fill` его пропускают. Группы `auto_batches` собираются по
  всем узлам намеренно: фильтр на сборке групп означал бы, что скрытый в этот момент
  узел без пересборки уже не покажется, а ради мгновенного скрытия задача и заводилась.
  `nodes()` и `sprites()` в стратегии на смерти больше не пересобираются.
- [x] **14.8. Снимок позиций для холодных читателей. ПРИНЯТ 25.09.2026.** `scene.snapshot()`
  отдаёт три числа на узел в порядке `nodes()` (у скрытых нули), `snapshot_count()` и
  реактивный `snapshot_version()`. Заполняется один раз за кадр в конце огороженного
  блока, версия это номер кадра. Мини-карта стратегии читает снимок, но перерисовку
  по-прежнему гонит таймером: подписка на версию перерисовывала бы сто точек DOM каждый
  кадр, что дороже выигрыша. Копия позиции из юнита ушла.
  Про `snapshot_version` помнить: это `@$mol_mem`, непрочитанную ячейку мол сметает, и
  спасает только то, что туда пишется номер кадра — после сметания значение восстановится
  тем же числом. Счётчик с нуля на этом месте поехал бы молча.
- [x] **14.9. Рамка и мини-карта в мире. ПРИНЯТ 25.09.2026, замерено и оставлено как было.**
  Обе вещи перенесены в мир линиями (рамка выделения четырьмя отрезками через
  `point.world`, мини-карта сто отрезков из `scene.snapshot()` плюс рамка карты, три
  батча на `shape/lines`) и замерены против DOM. Ожидание «в мире дороже» не
  подтвердилось, но и выигрыша нет.

  Замер A/B в одном браузере, страницы обоих вариантов подряд, Chrome headless с
  настоящим GPU (`--ignore-gpu-blocklist`, ANGLE Metal, M4 Pro), медиана трёх кругов по
  300 кадров, главный поток из `Performance.getMetrics`:

  | вариант | tick, мс | главный поток, мс/кадр | layout, мс/кадр | видов DOM |
  |---|---|---|---|---|
  | DOM, игра идёт | 5.25 | 5.8 | 0.1 | 125 |
  | мир, игра идёт | 5.23 | 5.7 | 0.1 | 24 |
  | DOM, пауза | 1.69 | 2.0 | 0.1 | 125 |
  | мир, пауза | 1.59 | 1.8 | 0.0 | 24 |

  Разница на уровне шума: кадр держат сто узлов сцены, а не интерфейс. Сто точек DOM
  стоят дёшево, потому что перерисовываются раз в 500 мс, а не каждый кадр.

  Главное, ради чего замер и делался: **на SwiftShader ответ противоположный и
  неверный.** В пробе (`--use-angle=swiftshader`) мир даёт 5.7 мс против 3.6 мс у DOM,
  потому что CPU-растеризатор платит за каждый отрезок, а на настоящем GPU это ничего.
  Любой замер рендера по пробе врёт в пользу DOM; пробы для пикселей, замеры для
  отдельного запуска Chrome с GPU (раздел 14 архитектуры).

  Решение: оставить DOM. Выигрыша нет, а цена перехода есть: на SwiftShader кадр
  дорожает на 2 мс при воротах пробы в 8 мс, и обе проверки пробы («рамка
  нарисовалась», «мини-карта не пуста») пришлось бы переписывать с элементов DOM на
  пиксели, то есть на более хрупкое. Код мирового варианта не сохранён, рецепт здесь.

- [x] **6.18. Мигающий сценарий комнаты. ПРИНЯТ 25.09.2026.** Проверка руки на костях падала,
  когда параллельно собираются другие бандлы, и проходит при одиночном запуске.
  Воспроизводится и без свежих правок, то есть это не регрессия, а слабое место самой
  проверки: она смотрит пиксель на конце руки в конкретный момент времени. Сделать её
  устойчивой (собрать множество цветов за окно кадров, как сделано с кадрами ходьбы
  героя). Полчаса.
  24.09.2026: падения CI «Страница не ответила за 30000 мс» к этой мигающей проверке
  отношения не имели. Правка `bog/probe` с биением к браузеру и растяжкой ожиданий
  лежала локально непушнутой, а CI тянет паки с их remote и собирался со старой версией.
  После починки вылезла сама проверка, и мигала она не по времени, а по порогу: фон в
  точке руки `[29,29,40]`, а `dark` требует каждый канал строго меньше 40, то есть
  падение решала единица в синем канале. Сделано: порог «не ярче фона» по сумме
  (`probe_dim`), а окно выборки руки считается кадрами с досрочным выходом, а не
  полутора секундами по стенным часам — на загруженной машине кадров в окно попадало
  слишком мало. Заодно переписана самая хрупкая из оставшихся проверок: «каркас белый»
  при пороге 200 и замеренных 230 сторожил константу кривой тон-маппинга, а не каркас.
  Разбит на три независимых утверждения — каркас серый по разбросу каналов (0 против 16),
  ребро без каркаса не серое (53), с каркасом ярче в линейном свете (5.8 при пороге 1.5).

- [x] **6.22. Честный замер рендера инструментом репозитория. ПРИНЯТ 25.09.2026.** Единственный способ
  правдиво замерить рендер — свой запуск Chrome с настоящим GPU, и он лежит в скретчпаде
  сессии, а не в репозитории. Под рукой у следующего окажется проба, которая
  систематически врёт в пользу DOM (см. раздел 14 архитектуры). Завести `bench/`: запуск
  Chrome без `--disable-gpu` и с `--ignore-gpu-blocklist`, две страницы подряд в одном
  браузере, медиана по кругам для `tick`, главного потока и числа видов DOM. В CI не
  включать, на раннере настоящего GPU нет. Тесты только на сборку командной строки,
  разбор счётчиков и медиану.
  Сделано `bench/`, три файла. Две вещи сверх задания, обе оправданы. **Инструмент сам
  печатает рендерер и ругается, если тот программный**: запустить замер без GPU легко, а
  отличить враньё по числам на глаз нельзя, то есть без этой проверки модуль повторял бы
  ровно ту мину, от которой заводился (замерено: 11.3 мс против 1.5 мс на Metal). И у
  страницы есть `dir`, подменяющий ей собранные `index.html` и `web.js`, иначе сравнить
  две правки одного приложения невозможно, а это основной случай. Тринадцать тестов, ни
  один не поднимает браузер; утверждение «в командной строке есть `--ignore-gpu-blocklist`
  и нет `--disable-gpu`» теперь стережёт тест. В разделе 14 архитектуры мёртвая ссылка на
  удалённый спайк S2 заменена на имя модуля.
- [x] **6.19. Демо «Плоский мир» на автобатчах. ПРИНЯТ 25.09.2026.** Из дерева ушли
  `Batch`, `Tilemap_batch` и `Coin_text_batch*`, из вида `batches()`, `tilemap_pool()` и
  `coin_text_pool()`: сцена берёт `auto_batches()`, порядок батчей повторяет порядок
  узлов, тайлмап и тексты «+1» попадают туда источниками. Минус 26 строк. Проба плоского
  мира зелёная.

- [x] **6.20. Цикл владения в мозгах. ПРИНЯТ 24.09.2026.** `owner` у машины и дерева
  поведения стал обычным методом поверх поля, как у боя; тест на голых объектах.
- [x] **6.21. Время в тестах целыми. ПРИНЯТ 24.09.2026.** Правило в STYLE.md вместе с
  правилом про компоненты и владельца.

## Этап 15. Уроки игры, сделанной через редактор

Игра `bog/crumb` (локально, без удалённого репозитория) собрана мышью в редакторе и дописана
руками. Из документа редактора в игру переехали девять строк карты и шесть пар координат,
руками написано 468 строк. Список ниже — очередь по важности, а не по порядку находки.

- [x] **15.1. Документ редактора должен собираться в MAM. ПРИНЯТ 25.09.2026.** Самое важное, без этого пути от
  редактора к игре не существует вовсе. Редактор пишет позицию списком (`pos / 10.5 -1.5 0`),
  а такой файл падает на аудите: `TS2344, Type 'readonly any[]' does not satisfy the constraint
  'Float32Array'`. То есть редактор нарушает собственное правило пака про вектор из дерева
  (STYLE.md, «Вектор из дерева»), и не замечает этого, потому что компилирует документ в
  браузере через `new Function` без проверки типов. Каждый поставленный мышью узел приходится
  переписывать руками.
  **Приёмка не «`pos` стал биндингом», а тест, который берёт документ, выданный редактором, и
  прогоняет по нему сборку.** Иначе правка отвалится молча при первом же новом свойстве.
  **Очевидная правка хуже нынешней поломки, проверено 25.09.2026 сборкой и рантаймом.** Если
  писать `pos <= Hero_pos Float32Array`, как велит правило пака, аудит становится зелёным, а
  сгенерированный код отдаёт `new Float32Array()` — буфер НУЛЕВОЙ длины. Тип сходится, значение
  выброшено, все узлы встают в никуда. Сейчас документ хотя бы громко не собирается.
  Причина: чистый `view.tree` не умеет выразить литерал `Float32Array` вовсе. Все игры пака
  пишут `<= что-то Float32Array` в дереве, а значение отдают методом в `.view.ts`; у документа
  редактора спутника на TS нет, поэтому он и жил только в браузере, где типов нет.
  **Решение: редактор экспортирует модуль из двух файлов** — дерево плюс спутник с
  методами-векторами. Не «минус», а признание: сцена и есть модуль, а не сниппет, и все четыре
  игры устроены именно так.
  **Вариант «база сама переведёт список в буфер» невозможен, а не дорог.** Литерал в дереве
  вешает на экземпляр собственную функцию, и базовый аксессор не выполняется вообще (раздел 14
  архитектуры). Остаточная форма «преобразует потребитель» отпадает по цене: потребитель `pos()`
  это горячий путь, чтение на узел за кадр, и за безусловную работу на каждом узле мы уже
  получили двукратную регрессию кадра (15.12).
  Сделано: `doc.module( klass )` отдаёт пару текстов — дерево, где каждый числовой список стал
  типизированным портом, и спутник на TS с самими числами. Правило выбора «числовой список»,
  поэтому строковые списки и ссылки на детей экспорт не трогает. Покрытие шире `pos`: в фикстуре
  лежат `pos`, `tint` и `scale`, потому что инспектор пишет их одним и тем же методом и болезнь
  была общая.
  **Приёмка устроена лучше, чем я просил.** В паке появился модуль `case/`: там лежит документ,
  выданный редактором, и рядом он же, вывезенный экспортом, **настоящими файлами** — значит их
  типы проверяет сама сборка пака, а не тест. Тест сверяет вывод экспорта с этими файлами. Новое
  векторное свойство, которое экспорт не умеет, краснеет дважды: сперва тестом на расхождении,
  потом аудитом на типах. Прогон TypeScript из теста отвергнут сознательно и вслух: `require`
  пакета втащил бы десятки мегабайт в тестовый бандл на каждый прогон.
- [x] **15.1а. Кнопка вывоза сцены модулем. ПРИНЯТ 25.09.2026.** На странице «Исходник» поле
  имени класса и две ссылки, «Вывезти дерево» и «Вывезти спутник»; имена файлов по последнему слову
  класса, по умолчанию корневой класс документа. Плохое имя **гасит ссылки, а не роняет страницу**:
  наверх пропускается только приостановка. Тест сверяет содержимое ссылок с выводом экспорта, а не
  форму строки.
  **Найдена ловушка в существующей кнопке:** «Скачать файлом» отдаёт документ, который как раз и не
  собирается, то есть пользователь мог воспроизвести исходную болезнь своими руками. Решено
  поведение не менять, а назвать назначение: это копия для открытия обратно в редакторе, а для пака
  нужны два файла модуля. Убирать кнопку нельзя (перенос между браузерами и работа без ленда),
  заставлять её отдавать модуль тоже нельзя (исчезнет способ унести и вернуть сам документ). Ловушка
  закрывается названием назначения, а не сменой поведения.
- [ ] **15.2. Работа не переживает перезагрузку.** Без ленда Базы исходник живёт в памяти
  страницы, экспорта в файл нет, единственный выход — скопировать текст из вкладки «Исходник»
  руками. Потеря работы хуже отсутствия возможностей: инструмент, который теряет сделанное,
  не используют.
- [ ] **15.3. Удаления, переименования и дублирования нет нигде.** Ни в дереве, ни в
  инспекторе, ни по клавише. Поставленный по ошибке узел убирается только правкой текста.
  Имя не редактируется потому, что `name` не входит в `props()` узла движка, и из-за этого пять
  поставленных крошек называются одинаково, а выбирать нужную приходится перебором.
  **Решение по имени: движок не трогаем.** `node.name()` уже есть, документ уже умеет строку
  `name \Крошка`, `doc.node()` уже предпочитает имя из документа имени класса, а `doc.set`
  уже генерирует такую строку. Не хватает единственного — поля в форме инспектора, потому что
  форма строится из `props()`, а `name` там нет и быть не должно. Та же линия, что с признаком
  происхождения свойства у префабов: редакторская забота остаётся в редакторе. Побочно чинится
  дерево сцены, его строка читает заголовок, а тот читает имя.
  Довод сильнее абстрактного: попади имя в `props()`, кнопка «Стоп» возвращала бы вместе с
  состоянием игры ещё и имя, то есть переименование во время игры молча отменялось бы.
  **Удаление пары «тело и вид» несимметрично, и место починки не в удалении.** Образец ставит герою
  тело `Hero` и вид `Hero_look`, который на тело ссылается строкой `parent <= Hero`. Удаление `Hero`
  отказывает словами «узел используется `Hero_look`, удалите его первым», иначе документ перестал бы
  собираться. Выходов четыре: (а) оставить пару и удалять в два приёма, а порядок назвать в
  сообщении; (б) удалять зависимых каскадом; (в) запретить виду ссылаться на тело; (г) снимать не
  узел, а постановку — тот, кто пару поставил, знает, что создал, и убирает её целиком. Взято (а):
  удаление остаётся тупым и предсказуемым, а каскад уносит чужую работу одним щелчком. Правильное
  место всё же (г): асимметрию заводит постановка, ей и снимать; вернуться к этому, когда у
  постановки появится своя запись о собранном.
- [x] **15.4. Свободная камера редактору. ПРИНЯТ 25.09.2026.** Колесо, панорама и «Показать
  всё» на движковых `pan`/`zoom_at` из 14.1. Множитель колеса — чистая функция от `deltaY` с
  нормализацией строчного и страничного `deltaMode` и зажимом: без неё щелчок трекпада в одном
  режиме давал бы совсем другой шаг, чем в другом. Панорама работает «хватанием мира»: мировая
  точка запоминается при нажатии, при движении берётся новая уже со сдвинутой камерой, и
  сдвигается на разницу — точка не уползает из-под курсора и ошибка не накапливается. «Показать
  всё» собирает рамку по той же геометрии, по которой работает выбор указателем. Пределы зума
  раздвинуты в дереве до 0.05 и 8: с умолчаниями движка карта 40×30 в кадр не влезает вовсе, и
  кнопка врала бы. Главный тест — мировая точка под курсором остаётся на месте после зума.
  Было: `Cam $bog_gamengine_cam_flat height 6` прибит:
  ни колеса, ни панорамы, ни «показать всё». При холсте 616×801 дальний угол карты 12×9 лежит
  по экранным координатам 1843×1535, то есть мышью туда попасть нельзя в принципе, и половина
  карты человеку недоступна. Движку свободную камеру сделали в 14.1 (`pan`, `zoom_at`,
  `cam/edge`), редактору она не досталась. Дешёвая задача с большим эффектом.
- [x] **15.5. Координата пишется с мусором и не липнет к сетке. ПРИНЯТ 25.09.2026.** Округление
  на стороне редактора, ровно там, где число уходит в документ, во всех четырёх местах: клик,
  перетаскивание файла, живая позиция при таскании гизмо и запись после отпускания. Переключатель
  «К сетке» включён по умолчанию, шаг 0.5; с выключенной сеткой округление остаётся до тысячной,
  потому что ставить предмет между клетками иногда нужно, а хвост цифр не нужен никогда.
  **Хвост рождается дважды,** и это важно для 15.1: сначала обратная проекция даёт `-4.500001`,
  потом `pos` лежит в `Float32Array`, и обычное чтение массива возвращает `5.300000190734863`.
  `doc.token()` режет до 1e-6 и вторую беду не ловит, поэтому округлять надо до записи, а не
  надеяться на формат.
  Было: Клик в центр клетки уехал
  в документ как `pos / 5.5 -4.500001 0`: число из обратной проекции пишется как есть.
- [x] **15.6. Ассет снимается с выбора после каждой постановки. ПРИНЯТ 25.09.2026.** Снятие
  убрано, выход из режима постановки теперь один и понятный: escape гасит и тайловый инструмент,
  и выбранный ассет, либо повторный клик по строке ассета. Три клика подряд дают три копии.
  Было: `pointer_down` ставит
  `asset( null )`, поэтому пять копий это пять походов в список. Ожидается одно действие на
  копию.
- [ ] **15.7. Тайловые инструменты знают только класс редактора.** «Клетка», «Прямоугольник»
  и «Заливка» проверяют `instanceof $bog_gamestudio_sample_map`, тайлмап движка редактор не
  понимает. Значит игра либо тянет пак редактора с его образцами, либо переносит карту текстом.
  Заодно: карту нельзя ни создать, ни расширить кистью, размер задаётся только текстом, и
  палитра тайлов берётся из `palette *` сцены, то есть новый вид тайла заводится тоже текстом.
- [x] **15.8. Палитра классов, и первым в ней тело физики. ПЕРВЫЕ ДВА ПУНКТА ПРИНЯТЫ 25.09.2026.** Панель ассетов это список
  **файлов**, а тело физики, управляемый герой и правило — **классы**, их там неоткуда взять
  по устройству. Нужна вторая палитра, а не больше видов файлов в первой.
  Порядок по тому, как оно блокировало на самом деле, а не по списку возможностей.
  **Первое: тело физики со стенами.** Это не «одно из», это единственное, что делает «Играть»
  проверкой, а не превью: герой прошёл сквозь стену на первой же секунде, и собранная мышью
  сцена перестала быть игрой. Одним классом в палитре не обойтись — телу нужна сцена с `phys`
  и `phys_tile` от той же карты, то есть **редактор должен уметь завести сцене мир, а не только
  положить узел**. Пока этого нет, остальное в палитре бессмысленно: проверить сцену игрой
  всё равно нельзя.
  **Второе: управляемый герой как класс.** Сейчас он один, `$bog_gamestudio_sample_hero`, и
  живёт в паке редактора, то есть в игру не переносится. Второе, а не первое, потому что
  образец хотя бы есть и ходить даёт.
  Место этому классу **в движке, а не в паке редактора**, и это подтверждено счётом, а не
  вкусом: одну и ту же форму «свойство `input` плюс чтение двух осей в скорость» руками
  написали четыре места — `bog/jumper/hero` (175 строк), `bog/shooter/player` (233),
  `bog/crumb/hero` (37, из них почти всё обвязка) и ходок комнаты (109). Примитив движка
  забирает у них общее ядро: `input`, `speed`, шаг, отображающий оси в скорость, со сторожем
  «нет ввода — остановиться» и «не изменилось — не писать». Прыжок, объём и поворот остаются
  у наследников.
  Место выбрано не на глаз: у движка уже есть объёмный `phys3/walker`, от которого наследуется
  игрок шутера, и плоский встал рядом по той же схеме имён. `input()` без своего значения берёт
  ввод у сцены, как у объёмного соседа, поэтому палитре не нужно писать связь в документ — на
  одну связь меньше, которую можно забыть.
  **Диагональ нормируется, решено 25.09.2026.** Во всех четырёх исходных местах `axis` даёт
  −1/0/1, и по двум осям выходит `speed × √2`, то есть по диагонали герой быстрее в 1.41 раза.
  В играх это считается ошибкой, а не особенностью. Чиним в примитиве сразу, **до** перевода
  четырёх мест: после перевода это была бы смена ощущения четырёх игр в произвольный момент, а
  сейчас это просто свойство нового класса. Нормировать только когда длина больше единицы, иначе
  сломается аналоговый ввод, где частичное отклонение стика законно.
  **Третье: прицепить компонент к узлу** — правило, бой, мозги. Третье, потому что без первых
  двух его не на чем проверять. **ПРИНЯТО 25.09.2026.** У узла появилось общее крепление `parts`:
  компонент цепляется к любому узлу, получает хозяина при чтении списка (поэтому строка `owner` в
  документе не нужна) и сливает свои свойства в `props()` узла с приставкой по виду
  (`combat.health`). Шага компонентам намеренно не добавлено: `props()` читает только инспектор и
  снимок «Играть», не кадр — прямое следствие регрессии 15.12, второй раз на те же грабли не
  наступаем. Незапланированное следствие: снимок «Играть» и откат «Стоп» подхватили компоненты
  бесплатно, потому что оба ходят по свойствам узла.
  **По дороге найдена поломка, которая убивала документ.** Инспектор пишет любое свойство в
  объявление **узла**, поэтому, как только компонент отдал свои свойства наружу, в документ уехала
  строка `combat.health_max? 70` на узел, компилятор дерева её отверг, и сцена перестала собираться
  вовсе. Цена была не «правка не сохранилась», а **одна правка числа убивает документ**. Починено
  маршрутизацией: свойство компонента пишется в объявление компонента.
  **Поймано живым прогоном, а не тестом, и тестом не ловилось по построению: в тестах никто не
  печатает в поле.** Это третий за смену случай того же рода — node-тест проверяет, что ветка
  работает, а браузер проверяет, что ветка достижима (раздел 14 архитектуры).
  Дальше текст над головой, звук по событию, частицы: порядок внутри хвоста не назначен,
  назначать его, не поработав, значит гадать.
  **Приёмка пройдена живьём, а не тестом.** Карта набрана, вкладка «Классы», один клик — и в
  документе появились тело, тайл от карты сцены, мир и строка у корня. «Играть», зажатая клавиша:
  ходок пошёл от 1.5 и встал на 4.6 при стене на 5 и теле 0.8, то есть упёрся в её грань. «Стоп»
  вернул на старт. Тот самый герой, который утром проходил сквозь стены, больше не проходит, и
  **в игре для этого не написано ни строчки кода.**
  Палитра классов — отдельная вкладка, а не расширение списка ассетов: ассет это файл, тело это
  класс. Постановка двухшаговая, мир заводится один раз, второе тело просто присоединяется к
  списку. Вся работа с документом живёт в модуле палитры и проверяется без браузера; в общих
  файлах девять строк дерева и тридцать семь строк вида.
- [x] **15.15. «Показать всё» не двигает камеру на сцене из одной карты. ПРИНЯТ 25.09.2026.** Рамка собирается по
  узлам, а тайловая карта не узел. То есть на сцене, где кроме карты ничего нет, кнопка молча
  ничего не делает — ровно в том случае, с которого человек начинает работу.
- [ ] **15.9. Правила остаются кодом, редактор цепляет компонент.** Решение принято, см.
  раздел 10.1 архитектуры. Мышью задаётся «какой узел, какие узлы, какое число», кодом —
  «что происходит, когда». Механика есть целиком (компонент с `props()` из 14.6, инспектор
  `props()` рисует), не хватает трёх вещей:
  **15.9а. Свойство-список узлов нечем заполнить.** Списочные поля инспектора строковые, то
  есть отдать правилу пять узлов можно только вписав пять имён, а имена не редактируются и у
  всех одинаковые (15.3). Две дыры складываются в стену. Нужен жест «выделил несколько узлов
  в дереве — отдал свойству».
  **15.9б. Показывать `props()` компонента во время «Играть», на чтение.** Почти бесплатно и
  меняет назначение инструмента: сейчас, чтобы узнать счёт, приходится читать текст подвала
  скриптом. С живыми значениями редактор впервые становится местом, где правило отлаживают, а
  не только расставляют.
  **ПРИНЯТ 25.09.2026 на настоящем правиле из игры.** Приёмка живая: поставил правило мышью, указал
  мышью героя и крошку, «Играть», прошёл вправо — крошка собралась (`взято 1`, `осталось 0`). Логика
  мышью не выражена, девяносто три строки остались кодом. Понадобился один новый вид свойства —
  одиночная ссылка на узел — и `props()` у самого правила; жест не изменился ни на строку, только для
  одиночной ссылки запись заменяет прежнюю вместо дописывания.
  **Находка про устройство важнее фичи: правило игры не может жить в палитре общего редактора,**
  иначе редактор зависел бы от игры. Решение — у каждой игры своя студия, наследник приложения
  редактора с дописанной палитрой: двадцать четыре строки, ни одной правки в общем редакторе. Отсюда
  запись в архитектуру, раздел 10.0: **редактор это библиотека, а не приложение.**
  **15.9в. Числа баланса ползунком.** Радиус подбора и лимит времени вслепую не выводятся, их
  щупают игрой; крутить ползунок ценнее, чем пересобирать бандл.
- [x] **15.10. Мигание дерева: НЕ ВОСПРОИЗВОДИТСЯ, закрыт 25.09.2026.** Замерено три пути по
  четырнадцать кадров: дерево не становится пустым ни на один кадр, оно либо прежнее, либо уже с
  новым узлом. Что-то из правок смены (сборка документа, липкий выбор ассета, палитра) мигание
  уже сняло. Кода не написано намеренно: **без теста, который краснеет сейчас, правка была бы
  догадкой, и завтра никто не узнал бы, работает она или нет.**
  Попутно выяснилось, почему мигание вообще было видно недолго: колода рисует ровно одну страницу,
  поэтому пока открыта вкладка ассетов, дерева сцены в DOM нет вообще. Наблюдать постановку, глядя
  на дерево, стало возможно только после 15.6, когда выбор ассета перестал сниматься.
  Единственная настоящая пауза: при переключении на вкладку сцены ровно один кадр страницы нет в
  DOM — это монтирование страницы колодой, лечится другим компонентом в левой колонке, а не правкой
  дерева. Отдельная задача, дороже получаса, заводить пока не за чем.
- [ ] **15.18. Поставленное, но невидимое в дереве.** Замерено: поставленный звук в дереве сцены
  не появляется вовсе (`3 3 3 3…` строк), хотя строка в документе есть. Значит его нельзя ни
  выбрать, ни переименовать, ни удалить кнопкой — только текстом, то есть ровно та жалоба, с
  которой начинался этап 15.
  **Инвариант, который отсюда следует и который важнее самого звука: что можно поставить, то можно
  выбрать, переименовать и удалить.** Поставленное и невидимое в дереве — ловушка: оно есть, но
  недостижимо.
  Звук при этом узлом делать **не надо**: в движке он компонент (`$mol_object2`), как бой, мозги и
  скин, и тащить его в узлы значило бы ломать решение раздела 10.1 ради удобства дерева. Правильно
  наоборот: **дерево редактора это список того, что объявлено в документе, а не зеркало
  `scene.nodes()`**, и компоненты в нём показываются под своим владельцем. Это же требование
  закрывает и прицепление правил (15.9): компонент, которого нет в дереве, нельзя ни выбрать, ни
  настроить.
  **Пересечение решено 25.09.2026 в пользу строк.** Параллельно компонент научился сливать свои
  свойства в `props()` хозяина с приставкой (`combat.health_max`) и маршрутизировать запись в своё
  объявление. Оба решения вместе дали бы два места правки одного числа, и одно тихо ломалось бы.
  Выбраны строки, потому что слияние закрывает только компоненты **с хозяином**, а поставленный
  звук хозяина не имеет вовсе — он и был исходной жалобой. Приставка и маршрутизация сняты.
  **Следствие, без которого решение неполное:** снимок «Играть» обходил `node.props()`, и компоненты
  попадали в него бесплатно именно из-за приставки. Снимок переводится на **тот же список, по
  которому строится дерево** — тогда компоненты покрыты по построению, и механизм один на дерево,
  инспектор и снимок. Это и есть настоящая причина выбора. Приёмка обязательна и отдельна: покрутить
  число компонента в «Играть», нажать «Стоп», получить прежнее. Без неё потеря молчит.

**Что за прогон не сломалось.** Ни одного исключения, холст не почернел ни разу, модель `.glb`
в плоской сцене встала без ошибки. Худший класс поломок тут не падения, а то, что редактор
зелёный всегда: про непригодность документа узнаёшь через полчаса, на сборке пака.

- [ ] **9.11. Авторские цвета-множители пересчитаны не все.** Грабля 3.14 применена к трём
  константам (`clear`, `fog_color` комнаты, порог блума), а тинты и цвета света остались
  экранными. Они тоже написаны глазами и тоже стали множителями в линейном свете, то есть на
  экране выходят бледнее и менее насыщенно, чем автор задумывал: множитель 0.4 после гаммы
  читается как 0.66.
  Найдено ведущим перебором, а не пробой, и это существенно: **все цветовые проверки пробы
  относительные** (ярче/темнее, тепло против холода), поэтому одинаковое побледнение всех
  участников они пропускают по построению. Тихая регрессия ровно того вида, который мы
  сегодня ловили трижды.
  Что не пересчитано, найдено грепом по авторским литералам:
  `demo/room` — `light_warm_color` `[1, 0.6, 0.3]`, `light_cold_color` `[0.3, 0.6, 1]`;
  `demo/flat` — `ghost_tint` `[0.7, 0.4, 1, 1]`, `coin_text_color` `[1, 0.92, 0.35, 1]`;
  `demo/spin` — `tint` `[1, 0.04, 0.04, 1]`;
  `bog/legion` — `mine_lit` `[1, 1, 0.45, 1]`, `flash_color` `[1, 0.92, 0.6, 1, 1, 0.35, 0.15, 0]`.
  **Решено 25.09.2026, по пиксельному замеру, а не по арифметике.** Тинты переводить: тинт это
  множитель альбедо, и автор целился в экранный вид. Замер подсветки выделения в стратегии дал
  множитель по синему ×0.79 при авторских ×0.45, то есть **подсветка ослабла примерно вдвое**;
  остаток до расчётных 0.70 съедают полупрозрачные края спрайтов. Восстанавливается возведением
  в 2.2: 0.45 → **0.172611** (моё «0.166» было посчитано неверно, проверено обратным ходом:
  `0.172611^(1/2.2)` даёт ровно 0.45, и показатель взят тот же, что кодирует тон-маппинг). Так же `flash_color` стратегии и `spark_color` шутера: хвосты 0.35 и 0.15
  выходят на экран как 0.62 и 0.42, то есть вспышка и искра не догорают до тёмного.
  **Свет и `ambient` не переводить.** Яркость и цвет света это линейные физические величины, их
  не «забыли перевести» — их подбирали на глаз до появления гаммы, поэтому правильное действие
  здесь перенастройка по картинке, а не возведение в степень. Движковый `ambient 0.35` и
  шутерные `ambient 0.2`, `power 0.6`, `power 1.1` в одних единицах, и править их по отдельности
  значит рассогласовать.
  `bog/jumper` авторских цветов не имеет вовсе и не затронут.
  Проверять картинкой и пиксельным замером: **все цветовые проверки проб относительные**, и
  одинаковое побледнение всех участников они пропускают по построению.
  **ПРИНЯТ 25.09.2026, замерено до и после.** Медиана множителя по синему на пикселях, которые
  спрайт кроет полностью (яркость выше 150 — именно они дают чистый тинт без разбавления
  полупрозрачными краями): было 0.727, стало 0.464, задумано 0.45. Прежняя оценка 0.79 по всем
  изменившимся пикселям была занижена разбавлением краями, медиана по кроющим честнее.
  Хвосты вспышки и искры сверены чтением значений с живой страницы: на экране ровно авторские
  числа, то есть 0.35 и 0.15 больше не выходят как 0.62 и 0.42 и догорают до тёмного. Пиксельно
  частицы не мерены (короткоживущие, нужен выстрел), и это сказано прямо: там измерено значение,
  а не пиксель.
  Авторские числа остались на виду в коде (`screen_tint( 1, 1, 0.45, 1 )`), возведение делается
  один раз при загрузке модуля, альфа не трогается. Помощник в четыре строки продублирован в двух
  играх сознательно: отдельный модуль движка ради четырёх строк не заводится, вынесем, если
  попросит третья игра.

- [ ] **15.11. Перевести четыре места на примитив ходока.** `demo/jumper/hero` (175 строк),
  `demo/shooter/player` (233), `bog/crumb/hero` (37), ходок комнаты (109). Первые два переехали
  внутрь движка этапом 16.1: искать их по старым путям `bog/jumper`, `bog/shooter` больше негде.
  Отдельной задачей и
  после приёмки палитры, чтобы не тащить четыре пака внутрь задачи про редактор. У каждого
  наследника остаётся своё: прыжок, объём, поворот. Ожидается смена ощущения по диагонали —
  это починка, а не регрессия; пробы игр прогнать и числа сверить.

- [x] **15.12. Кадр стратегии вырос вдвое от скрытия узлов и снимка.** Замер `bench` на
  настоящем GPU: 7.90, 7.70, 7.70 мс при разбросе 0.2 мс, тогда как утром было 3.44 и в плане
  записано 3.79 при 911 узлах; сейчас узлов 912, то есть работа та же. Загрузкой машины это не
  объясняется: она была 3.4–4.5 при двенадцати ядрах, а узкий разброс даёт не загрузка. В одном
  процессе: шутер 0.60, прыгун 1.60, стратегия 7.20 — пострадала только игра с почти тысячей
  узлов. `layout` 0.08 при `tick` 7.20, значит это холодная зона, а не рендер и не DOM.
  Источник — `d8c3059`, две вещи на каждый узел каждый кадр. `if( node.shown() ) node.step( dt )`,
  а `shown()` идёт вверх по родителям через мемо-атом `parent()`. И безусловный `snap_fill`,
  который зовёт `shown()` второй раз и берёт `world()`. **Стратегия снимками не пользуется
  вообще, но платит за 912 узлов дважды за кадр.**
  Это нарушение главной ставки (раздел 1: холодная зона дорогая на штуку, около 1.3 мкс за
  объект): добавлены два безусловных реактивных прохода по всем узлам. Принято ведущим утром без
  замера — вина общая.
  Направление: снимок заполнять лениво и только когда его читают; `shown()` не звать второй раз;
  подумать про цену самого `shown()`, подъём по мемо-атомам на узел на кадр дорог и однократно.
  **ПРИНЯТ 25.09.2026.** Снимок стал ленивым: заполняется мемо-методом при первом чтении в кадре,
  и стратегия читает его дважды в секунду по таймеру мини-карты вместо шестидесяти. Видимость
  считается один раз за кадр плоским проходом и кладётся полем узла; подъём по родителям через
  мемо-атомы из горячего пути исчез совсем, таблица родителей строится индексами один раз на
  изменение списка узлов. `shown()` остался честным медленным методом для холодных читателей.
  Нашёлся **третий** проход, которого не было в разборе: `batch.fill` тоже спрашивал видимость
  через атомы на каждом рисуемом узле, ещё около шестисот узлов за кадр.
  **Приёмка числом была сформулирована неверно, и это поправка ведущего к себе.** «Обратно к
  3.4–3.8 мс» смешало числа двух инструментов: 3.44 снято пробой на программном растеризаторе, а
  7.7 — `bench` на Metal. Верный контроль — A/B двумя сборками в одном браузере, где «до» это тот
  же код с проходами, выключенными в ноль: 5.20 против 5.10 мс, то есть от проходов не осталось
  измеримой цены, а регрессия была настоящая (7.7 → 5.1 на одном инструменте).
  **Профиль после фикса:** в горячей части нет ни одного кадрового прохода, всё время в трафике
  атомов самих юнитов. Дальнейшее уменьшение кадра стратегии — это перевод юнитов в буфер
  (раздел 1 архитектуры), а не правка сцены. Искать экономию в сцене больше нечего.
  Тест на снимок поймал ошибку автора в самой правке: буфер возвращается той же ссылкой, поэтому
  мемо версии не инвалидировалось, и версия обязана читать номер кадра напрямую.
  **Два правила отсюда.** Любая правка, добавляющая проход по всем узлам, замеряется до приёмки.
  И приёмка числом обязана называть инструмент: **число без названного инструмента это не приёмка,
  а пожелание**, потому что два наших инструмента расходятся вдвое на одном и том же коде.

- [ ] **15.13. Отмена в редакторе.** Отмены нет вовсе, а сохранение стало мгновенным, поэтому
  ошибочное действие сохраняется тем же кадром, и единственная защита пользователя — экспорт
  файлом. Это не кнопка, а модель истории документа, поэтому отдельной задачей: пристёгивать её
  к удалению узла значило бы либо сделать её плохо, либо остановить удаление на день.
  Клавиша `Delete` сделана со стражем по целевому элементу, и страж нужен и при наличии отмены:
  `$mol_hotkey2` не смотрит на целевой элемент, а внутри корня лежат поле имени, числовые поля и
  textarea исходника, то есть без стража `Delete` при наборе имени удалял бы узел.

- [ ] **9.12. Подпись сцены как сторож от тихих цветовых регрессий.** Все цветовые проверки проб
  относительные по назначению: они сторожат переворот («грань к свету ярче тени») и пропускают
  равномерное побледнение всех участников по построению. Абсолютные пороги переводу не поддаются
  (грабля 3.14). Значит нужен третий инструмент, а не подкрутка имеющихся: именно этот класс
  регрессии сегодня проскочил мимо всех проб, и нашёлся перебором авторских литералов, то есть
  случайно.
  Подпись считается по всему кадру одним чтением пикселей, пять чисел на сцену: медиана яркости,
  10-й и 90-й процентили **отдельно** (равномерное побледнение двигает оба и не двигает ни одно
  отношение), доля провалов и выжженных (чтобы «настроили яркость» не означало «утопили тени»),
  средняя насыщенность как разброс каналов. Плюс именованные поверхности, которые проба комнаты
  уже щупает, и пара на арене шутера. Свечение — отдельной сценой, порог блума считается частью
  той же системы.
  **Назначение — сторож, а не цель настройки.** Подпись нынешнего состояния фиксируется как
  текущее значение с датой и коммитом, и проверка сторожит уход от него, а не близость к чему-то
  правильному.
  Инструмент один раз сверить на настоящем GPU против пробы: если расходится больше чем на пару
  единиц, доверять Metal. Сегодня три инструмента подряд молчали не потому, что всё хорошо, а
  потому что не умели мерить то, о чём их спрашивали.
  **ПРИНЯТ 25.09.2026, и с поправкой к моему же утверждению.** Допуски, которые я утвердил
  (медиана ±8%, точки ±12%), **пропускают настоящую регрессию**: правка движкового `ambient`
  0.35 → 0.3 прошла зелёной, потому что медиана комнаты сдвинулась всего на 3%, а точка тени на
  8%. Заменены на абсолютные, по замеренному шуму, а не по проценту: уровни ±1, доли ±0.005,
  насыщенность −0.2 (рост уходом не считается), точки ±3 по каналу. Основание — воспроизводимость:
  два прогона на одном рендерере дают одинаковые числа, два разных расходятся не больше чем на 1
  по каналу. После замены та же правка `ambient` даёт четыре независимых сигнала. Цена: намеренная
  правка картинки будет краснеть, и подпись придётся переснимать — это и есть работа сторожа.
  **Сцена для подписи обязана быть воспроизводимой.** Сцена «Ящики» выкинута: куча из трёхсот тел
  складывается по-разному от прогона к прогону (`low` 12 против 55, доля провалов 0.196 против
  0.035, точка попадает то в кирпич, то в пол), и подпись ловила бы раскладку кучи, а не цвет.
  Свечение вынесено отдельной сценой, и её светлый конец — самое чувствительное число из всех
  (128 против 98 у обычной комнаты), то есть «сломали свечение» станет видно сразу.
  **Эталон старой сборкой отвергнут 25.09.2026.** Старый кадр это не эталон, а то, что забраковано
  задачей 9.9: ACES без гаммы давал пересвеченную и блёклую комнату, и числа света подбирались под
  сломанный конвейер. Свести новую подпись в коридор вокруг старой значило бы вернуть брак с видом
  доказанного. Настройку света и `ambient` не делаем, пока нет жалобы на картинку от человека:
  свидетельства о поломке есть только по тинтам (замерено), а по свету только вывод «подбирали до
  гаммы». Когда жалоба появится, подпись сделает настройку измеримой.

- [x] **15.14. Привязку к сетке не проверяет никто. ПРИНЯТ 25.09.2026.** Основная проба редактора ждала переноса
  гизмо на `80 / ppu` = 0.698, а привязка к сетке (галка включена по умолчанию) округляет до 0.5,
  поэтому проба была красной в HEAD. Починена снятием галки перед переносом, а не ослаблением
  порога: проверка про гизмо, ослабленный порог перестал бы ловить поломку переноса. Но теперь
  саму привязку не сторожит ничто. Нужна своя проверка: с галкой узел встаёт на кратное 0.5, без
  галки — в точку клика с обрезкой до тысячной.

- [ ] **15.16. Жест «выделил несколько узлов — отдал свойству».** Проверено практикой 25.09.2026:
  это предусловие **правил** (15.9), а не прицепления компонентов. Бой цепляется к одному узлу и
  списка не требует, а список нужен правилу, которому отдают пять собираемых крошек. Моя прежняя
  оценка «без жеста прицепление бесполезно» была неверной.
  Суть: списочные поля инспектора строковые, поэтому отдать правилу пять
  узлов можно только вписав пять имён. Имена стали редактируемыми (15.3), так что стена ниже, но
  связать компонент с группой узлов мышью всё равно нельзя. Пока жеста нет, компонент можно
  поставить и нельзя связать, то есть третий пункт 15.8 бесполезен.
- [ ] **15.17. Отмена: остаток решений.** Решения приняты 25.09.2026, см. отчёт в истории:
  стек снимков исходника (все мутаторы сходятся в одну воронку `source( текст )`, поэтому снимок
  не расходится с документом, а список обратных операций ломается молча, когда автор нового
  мутатора забыл обратную), зернистость по жесту указателя плюс склейка набора по 600 мс (гизмо
  пишет на каждое движение), история не переживает перезагрузку и подвал говорит глубину (250 КБ
  в хранилище сделали бы ненадёжной запись там, где она работает). Отмена запрещена, когда в ленде
  есть другие люди: снимок затирает чужую одновременную правку, у Базы своя история. Повтор
  делается сразу, отмена без повтора это ловушка. При упоре в глубину кнопка гаснет, а не
  нажимается без последствий.

- [ ] **9.13. Подпись сцены для трёх игр.** Сторож (9.12) снимает подпись с демок движка, а
  цветовая регрессия случилась **в играх**: подсветка выделения в стратегии ослабла вдвое, и нашлось
  это перебором авторских литералов, а не инструментом. То есть дыра, ради которой заводился сторож,
  в играх остаётся открытой. Снять подпись со стратегии, шутера и прыгуна и зафиксировать как
  текущее состояние. Сцена обязана быть воспроизводимой, поэтому у стратегии брать кадр до начала
  боя, а у шутера — до первого выстрела: иначе подпись поймает раскладку, а не цвет (так уже
  выброшена сцена с кучей ящиков).

- [ ] **15.19. Пройти путь целиком: мышь → модуль → игра.** Этап 15 появился из того, что пути
  «поставил мышью — положил в игру» не существует: в игре `bog/crumb`, собранной мышью, из документа
  переехали девять строк карты и шесть пар координат, а **468 строк написаны руками**. За смену путь
  закрыт по частям (вывоз модулем, кнопка, палитра с телом и миром, компоненты, камера, округление,
  сохранение), но целиком его никто не прошёл.
  Собрать сцену той же игры заново мышью, вывезти модулем, положить в новый пак, собрать, поиграть.
  **Приёмка — число дописанных руками строк против прежних 468.** Близко к прежнему значит закрывали
  не то; упало в разы значит этап сделал, ради чего заводился. Правило игры (93 строки) считать
  отдельно: оно кодом по решению раздела 10.1, а не по нашей неспособности.
  По дороге ничего не чинить, записывать и обходить.

- [x] **15.19. Путь целиком пройден 25.09.2026.** Игра `bog/crumb2` собрана мышью, вывезена модулем,
  запущена, играется.
  **Число:** руками дописано **166 строк против 272** за то же место. Сцена: было 262 строки руками,
  стало 101 строка из редактора плюс две правки руками. Из игры исчезли карта строками, блоки тайла,
  атласа, физики, сцены, тайлмапа, героя и спрайтов, таблица точек крошек, пять мемо позиций и весь
  класс героя — его заменил «Ходок» из палитры, поставленный одним кликом вместе с миром физики.
  Осталось руками: страница с HUD, камера, раскладка клавиш, пауза, перезапуск, обращение к узлам по
  именам, склейка правил со сценой, и 93 строки правил, которые кодом по решению 10.1.
  **Предупреждение к этим числам, найдено 25.09.2026 при подготовке перезамера: 468 и 272 — это один
  и тот же `bog/crumb`, посчитанный двумя разными охватами, и охват нигде не назван.** Постановка
  говорит «468 строк написаны руками», приёмка сравнивает «166 против 272 за то же место», и различает
  их только оговорка «за то же место». Из перечня оставшегося руками видно, что в 166 входила
  **цена пака** («страница с HUD»), поэтому сравнимо с прошлым только «долг редактора плюс цена пака»;
  чистый долг редактора сравнивать не с чем, он новая база. Отсюда правило для любого следующего
  замера: **охват объявляется до замера, а не выводится из формулировки задним числом.**
  **Охват восстановлен построчно 25.09.2026, и тесты в одно из чисел всё-таки входят.** 272 это
  `app.view.tree` 104 + `app.view.ts` 120 + `hero/hero.ts` 37 + `index.html` 11 у `bog/crumb`; 166 это
  66 + 87 + 11 плюс две правки в вывезенном модуле у `bog/crumb2`. В этой паре тестов и правил нет, и
  она сравнима. А 468 это те же 272 **плюс** правила 93, тест правил 63 и `README` 40 — то есть в них
  вошли и тест, и документация. На этом широком охвате `crumb2` даёт 416, то есть «468 → 416». Обе
  пары честные, и вместе они говорят правду: выигрыш сидит в сцене и обёртке, а не в объёме пака,
  потому что правила и тесты никуда не делись и деться не должны. Числа сошлись до строки, поэтому
  сравнение с 272 остаётся, а 468 упоминается с названным охватом, а не как «посчитано иначе».
  **Это ошибка приёмки, а не неточность формулировки.** Критерием 15.19 стояло «число дописанных руками
  строк против прежних 468», то есть сравнение с числом другого охвата, и записал его тот же человек,
  который рядом записывал оба числа. Ошибку нашли через сутки, и не глазами, а построчной арифметикой.
  Критерий, сравнивающий с числом неизвестного охвата, пропускает и провал, и успех: он того же рода,
  что зелёный тест на сломанном поведении — форма есть, различающей силы нет.
  **Протокол перезамера, утверждён 25.09.2026.** Три ведомости вместо одного числа: (1) **долг
  редактора** — строки, которых редактор не дал, и каждая называет **отсутствующую возможность**, а не
  житейскую причину («вывоз не отдаёт ввод», а не «пришлось дописать клавиши»), это новая база и
  одновременно список задач; (2) **цена пака** — `index.html`, объявление класса приложения, корневая
  страница: одинаковы для любой игры, из сравнения исключены и названы кандидатом на вывоз, потому что
  обёртка, из которой получается запускаемый пак, вполне вывозима; (3) **решено кодом** — правила игры
  по решению 10.1. Строка руками это непустая строка, не пришедшая из вывоза байт в байт; пустые и
  строки из одних закрывающих скобок не считаются в обе стороны. Тесты вне счёта, объём назван отдельно.
  Список долга получается `diff`-ом сохранённой **до первой правки** выгрузки против запущенной игры, а
  не глазами. Вкладка «Исходник» под запретом; каждое честно вынужденное обращение к ней записывается с
  причиной, и их счёт — прямая мера того, где мышь не дотягивает. Попутно замеряется **время прохода по
  часам**: из строк иначе выведут «редактором быстрее», а это другое утверждение. Игра и место те же,
  любое отличие состава называется явно.
- [x] **15.20. Вывезенный `pos` односторонний: тело, поставленное мышью, рождается неподвижным.**
  Самый важный разрыв из прохода 15.19. Вывоз пишет `pos <= Walker_1_pos Float32Array`, а физическому
  телу нужен двусторонний `pos? <=> Walker_1_pos?` — это **записанное правило самого пака**
  (`STYLE.md`, «Физическое тело в дереве»), и вывоз про него не знает. Игра собралась с первого раза
  и не поехала: в подвале `Ходок: pos is read-only, declare it as 'pos? <=>'`. Чинится двумя строками:
  биндинг в дереве и `next?` в мемо спутника, но вывоз должен делать это сам для каждого свойства,
  которое пишет физика или шаг.
  **ПРИНЯТ 25.09.2026.** Вывоз пишет двустороннее связывание **всегда** для всех векторов, которые
  и так переписывает, а не по списку имён и не по виду свойства: список устарел бы на первом новом
  свойстве, а виды знает движок, тогда как вывоз работает до рантайма. Двусторонне безвредно там, где
  никто не пишет: чтение отдаёт литерал, запись сохраняет. Приёмка сменена на запуск, и фикстура
  держит обе формы под охраной.
  **Негативный прогон доказал вывод буквально: с односторонней формой аудит остался ЗЕЛЁНЫМ, а тест
  запуска упал** — `pos` остался 3.25 вместо 6.25, запись проглочена молча. Сборка этого поймать не
  может по построению.
  **Урок про приёмку, он дороже самой правки.** Приёмка 15.1 была «документ собирается», и она
  достигнута, а игра при этом не движется. **Собирается это не работает.** Приёмкой вывоза обязан
  быть запуск вывезенного модуля, а не его сборка. И шире: правила, записанные в `STYLE.md`, ничем не
  проверяются — оба провала вывоза (список вместо буфера в 15.1 и одностороннее связывание здесь) это
  нарушения собственных записанных правил пака.
- [x] **15.21. Вывезенный уровень тянул пак редактора. ПРИНЯТ 25.09.2026.** Документ наследовался от
  образцового класса студии, поэтому вывезенный модуль зависел от `bog/gamestudio` со всеми его
  образцами. Игра работала, но тащила студию. Тот же диагноз, что 15.7, только про корень любого
  документа, а не про тайловые инструменты.
  **Корень документа теперь `$bog_gamengine_scene`,** а всё, что образцовый класс делал за документ,
  выписано в сам документ движковыми узлами: `phys` с `phys_tile`, `tilemap` с палитрой, тело героя
  `phys_walker` и его вид спрайтом. Редактор при этом не потерял ничего: карту он ищет не в корне, а
  у того узла, который её держит (`map_owner`), а инструменты тайлов работают с движковой плоскостью.
  Платой стала пара «тело и вид» вместо одного узла — несимметричность удаления разобрана в 15.3.
  Приёмка не сборкой, а запуском, как требует 15.20: вывезенные `level.view.tree` и `level.view.ts`
  положены в отдельный пак, который про студию не знает, аудит зелёный, в ноде нажата `D` и прокручено
  три кадра — герой ушёл правее -2, «Монета» на месте, батчи собраны, — и в бандле ноль вхождений
  `gamestudio`. Пак после приёмки убран.
- [ ] **15.22. Липкий ассет перебивает кисть.** После 15.6 выбор ассета не снимается, а в обработчике
  нажатия ветка ассета стоит раньше ветки кисти: пока ассет выбран, кисть молча не красит, клик
  ставит спрайт. Обходится Esc. Лечится порядком веток либо снятием ассета при выборе тайла.
- [ ] **15.23. Игра обращается к узлам по именам, а не по роли.** Редактор называет узлы `Walker_1`,
  `Sprite_1…Sprite_5`, и игра из прохода 15.19 зовёт их поимённо: `Scene().Walker_1()`, список
  собираемых — перечисление пяти вызовов. Редактор не даёт игре ни роли «герой», ни списка «крошки».
  Следствие важнее неудобства: **сцена не переносится между документами**, потому что игра привязана
  к именам, которые редактор выдал по порядку постановки. Переименование узла (15.3) ломает игру
  молча. Нужен способ помечать узел ролью и отдавать игре список по роли — тогда игра зависит от
  смысла, а не от порядка кликов. По важности сразу после 15.20.
  **Движковая половина ПРИНЯТА 25.09.2026.** `role()` у узла строкой и в `props()` видом `text`,
  `by_role( role )` списком в порядке сцены, `by_role_one( role )` с броском на нуле и на двух и с
  числом узлов в сообщении. Громкость в движке, а не у потребителя: отказ «нет узла с ролью hero»
  одинаков для всех игр, каждая написала бы одно и то же, и одна однажды написала бы безопасное
  обращение вместо броска — метод с таким именем делает громкий путь коротким.
  Уточнение, которое перевернуло постановку: **имён у узла уже два** — заголовок, который правит
  пользователь, и имя объявления, которое редактор выдаёт по порядку постановки. Роль не третье имя,
  она заменяет то, которое пользователю не принадлежит.
  **Индекс ролей не заводим, и вот при каком условии стоит.** Подписки у мемо по ключу и у общего
  индекса одинаковы слово в слово, потому что индекс тоже обязан прочитать роль каждого узла. Индекс
  покупает только множитель числа ролей ценой второго механизма: для стратегии это 2700 чтений против
  900 на пересчёт, а пересчёт случается на действие человека, а не на кадр. Заводить его стоит на
  десятках ролей или на ролях, меняющихся каждый кадр — тогда замер займёт пять минут.
  Роль в снимке «Играть» не исключается сознательно: правило «какие свойства не снимаем» через месяц
  разойдётся с жизнью, лучше тупой полный снимок.
  **Редакторская половина ПРИНЯТА 25.09.2026, и она почти пустая — это признак верного выбора.**
  Роль строка, поэтому инспектор получил поле бесплатно от вида `text`, вывоз роль не переписывает и
  трогать его не пришлось, а дерево, адресация путём и `shared()` про роль не знают, как и решено.
  **Приёмка та, что и требовалась: не «метод что-то вернул», а второй документ.** В фикстуре два
  документа с одними ролями и разными именами объявлений и заголовками; тест находит героя и обе
  крошки в обоих и отдельно проверяет, что имён из первого во втором нет вовсе. Плюс тест на громкость
  обоих отказов по тексту сообщения.
  Негативный прогон показал две линии защиты: убранная строка `role` сперва покраснила сторож
  синхронности (вывезенные файлы разошлись с документом), а после перегенерации — ролевую приёмку.
  То есть роль нельзя потерять молча ни в документе, ни в вывозе.

## Этап 16. Один репозиторий

Решение пользователя 25.09.2026: **отдельных репозиториев у редактора и игр не будет, всё живёт в
`bog/gamengine`.** Причина, по которой это важно не только для порядка: сборка в CI тянет паки с их
удалённых репозиториев, поэтому пак без remote не попадает ни в прогон, ни в Pages — то есть у
редактора не может быть ссылки, пока он снаружи.

Цена переноса: MAM привязывает имя класса к пути, поэтому пак, переехавший внутрь движка,
переименовывается целиком. Померено: у редактора 109 уникальных имён, у шутера 35, у прыгуна и
стратегии по 18, у игр, собранных через редактор, по 6. Около 190 имён в 75 файлах.

- [ ] **16.4. Пропуск пробы принимается за успех во ВСЕХ шестнадцати местах рабочего дерева.**
  Найдено 25.09.2026 одним грепом сразу после того, как правило было сформулировано на одном случае.
  `$bog_probe_skip` это «Chrome не найден», и каждый тест пробы утверждает `skip || ok` — в паке движка
  шесть в `probe`, по одному в `look` и `net/probe`, два в `pack`, плюс подписи и пробы трёх игр, плюс
  шесть в студии. Значит **если Chrome перестанет находиться, все шестнадцать станут зелёными, и CI
  отчитается об успехе, не проверив ни одного пикселя и ни одного клика.**
  Сегодня дыру закрывает случайность: в `deploy.yml` есть строка `test -x` по пути Chrome, и поток
  падает раньше тестов. Это одна строка в одном потоке; у других потребителей `bog/probe` её нет.
  Отдельный случай той же формы, но без всякой страховки: у живой пробы студии мастер Базы на 9090, и
  тест утверждает `skip || no_master || live_ok`. На раннере мастера нет никогда, поэтому «второй
  редактор видит правку первого через ленд» зелёный всегда.
  **Чинить по правилу «пропуск решает тест, а не проба»** (STYLE.md): проба сообщает, чего не хватило,
  а приемлемо это или нет решает утверждение. И вторая половина, без которой починка пустая: **нужно
  место, где написано «здесь Chrome обязателен» и «здесь мастер обязателен»** — сейчас такого места нет
  ни в потоке, ни в тесте, ни в пробе, поэтому тест решал бы по тому же отсутствию ответа. Объявлять
  должен тот, кто называет прогон проверкой, то есть рабочий поток; локальный прогон без объявления
  пропускает и говорит об этом.
  Приёмка: спрятать Chrome и убедиться, что при объявленной обязательности краснеет, а без объявления
  пропускается с внятной строкой. Шестнадцать мест перевести разом, иначе правило останется в одном.
  - [ ] **`pack` не проверяется в CI ничем, и это третий случай отсутствующей проверки за вечер.**
    Найдено 25.09.2026 при переводе шестнадцати мест: у модуля `pack` дев-сервер вообще не производит
    `-/node.audit.js` (запрос отдаёт 404 сколько угодно раз, хотя `web.audit.js` собирается), и в
    списке `modules` рабочего потока модуля нет. Значит ни аудит, ни его два теста в CI не идут, а
    выглядит это как «пробы упаковщика есть». Решить два вопроса: почему у модуля нет node-аудита, и
    ставить ли `pack` в `modules` (его тесты зовут Chrome, то есть подорожает прогон).
  - [ ] **Поднять мастер Базы шагом рабочего потока и вернуть парные модули в `modules`.** Настоящее
    закрытие дыры: сейчас `studio/pair` и `net/pair` вне прогона, то есть совместная правка редактора и
    задержка сети в CI не проверяются вовсе, а только описаны в README. Цена названа: в поток добавляется
    живой сервис (мастер на 9090 плюс патч `masters()`, иначе узел ходит на прод), прогон дорожает на два
    браузерных сценария с тремя раундами каждый, и раннер начинает зависеть от Гипер Базы. Решение за
    Кириллом: это бюджет прогона, а не форма пропусков.
  - [ ] **Две грабли выноса, обе замерены 25.09.2026, чтобы следующий не тратил на них время.**
    Первая: **путь подпапки, совпавший с ИМЕНЕМ, которое родитель уже называет, превращает ссылку на
    функцию в ссылку на модуль.** Тест, положенный в `studio/probe/live/`, уехал в бандл родителя, потому
    что родитель называет `$bog_gamengine_studio_probe_live`; с каталогом `pair` вхождений ноль. Каталог
    сам по себе ничего не тянет, тянет совпадение имени. Приёмка выноса поэтому одна: **ноль вхождений
    имени в бандлах всех модулей из `modules:`, по каждому отдельно.**
    Вторая: модуль из одного тестового файла **без своих мол-имён** в корне пака падает на
    `draw.slots is not a function`, а в `studio/pair` собирается и проходит 1299 тестов — значит «нужна
    обёртка с именем» зависит от места, а не универсально. Замер повторён на спокойном дереве.
    **Третье утверждение, стоявшее здесь раньше, снято как выдумка:** «порядок кусков зависит от места, и
    от этого падает тест часов движка» — красным был `phys3`, он мерит себя `performance.now()`, и ни
    порядок, ни заглушка времени на него не влияют; три конфигурации различались не местом, а временем
    сборки, две из них попали в окно чужих правок физики. На текущих исходниках `studio/probe/pair` тоже
    зелёный, 1300 тестов.
  - [ ] **Известная плата за подписи игр в CI: дубль всех тестов игры.** Модуль подписи тянет родителя
    принудительно, поэтому его прогон повторяет 1409 тестов игры, которые уже идут внутри
    `demo/<игра>/probe`; аудит самой подписи при этом стоит 80 строк. Поэтому в списке одна подпись, а не
    три. Лечится раскладкой модулей, а не списком, и сейчас не лечится намеренно.
  - [ ] **Слепое место подписи: узел рисуется, но не шагает.** Замерено 25.09.2026 тремя поломками копии
    бандла демо. Убрать спрайты из рисуемого списка — подпись краснеет на трёх независимых числах, включая
    именованную точку (`hero 161,214,230` против `220,58,58`). Заглушить инстансные draw-вызовы — краснеет
    ещё громче. А вот убрать те же спрайты из `kids`, оставив в батче, — **зелёная, числа совпадают до
    последнего знака**, и это верно: спрайт теряет `step`, но рисуется по-прежнему, картинка та же.
    Значит подпись сторожит картинку, а не состав сцены, и «узел перестал жить» ей невидимо по построению.
    Закрывается не подписью, а пробой. Это ровно та поломка, что была у `crumb2` в другом изводе: уровень
    в сцену не попадал, а картинку рисовал чужой класс.
    **ЗАКРЫТО 25.09.2026, и предписанная здесь форма оказалась неверной.** Стояло: «утверждение вида „за N
    кадров хотя бы один узел сдвинулся“». Замер показал, что именно названный случай оно **не ловит**:
    спрайт, выпавший из `kids`, берёт трансформ у родителя-тела, а тело шагать не переставало — поэтому и
    картинка та же, и состояние то же, узлы движутся, утверждение зелёное (`moved 12`, `max 1.002`). Две
    другие поломки оно ловит: выключенный `step` врага и убранные из `kids` враги дают `moved 0`.
    Названный случай закрывает утверждение о **составе**, без зашитых чисел: сравниваются два списка
    самого продукта — `Batch().nodes()` против обхода `Scene().nodes()`, «всё, что батч рисует, сцена
    обязана шагать». В здоровом дереве пересечение полное, на поломке `76 из 76` лишних.
    Проверка поставлена в прыгуна: это единственная демка, где движение есть **без ввода** (три врага с
    мозгом-автоматом), у стратегии юниты идут по приказу, у шутера игрок по клавише. Во всех трёх играх
    смещение **по вводу** уже проверялось; не было нигде двух вещей — движения без ввода и «рисуется, но
    не шагает».
    **И оговорка про саму подпись.** Заморозку врагов она ловит одним числом на 2 единицы (`low 170`
    против софтовой базы `172` при допуске ±1), а её же эталон держит `172` для софта и `170` для GPU.
    Сначала я записал это как случайное попадание и как «допуск сопоставим с разбросом между машинами».
    **Оба утверждения неверны, проверено арифметикой по записанным базам 25.09.2026.** Сверка идёт не
    между машинами, а **с базой своей машины** (`kept = family === 'soft' ? base.soft : base.gpu`), а на
    нераспознанном рендерере проверка вообще отказывается сравнивать. Значит разброс между машинами допуск
    не съедает, и заморозку подпись поймала законно. Совпало только то, что сигнал поломки вышел того же
    размера, что разница между машинами.
    **Под вопросом не механизм сторожа, а разрешающая способность числа**, и вот она замерена. Из 63 чисел
    подписи у 43 разница между машинами ровно ноль. Разница ≥ допуска у **пяти**, и все пять — процентили
    (`low`, `median`, `high`), то есть 28 % этого семейства против 0 % у точек, долей и насыщенности.
    Механизм фрагильности назван: процентиль это порог по счёту пикселей, и когда у гистограммы около
    метки плато или провал, горсть пикселей выносит ответ на несколько уровней. У прыгуна кадр — плоское
    небо (`median` и `high` равны 204), метка 10 % падает на кромку земли и героя, где сглаживание у
    рендереров разное, отсюда 2 уровня на ровном месте. У шутера и стратегии процентили разведены далеко,
    и там разброс нулевой.
    **Отношение вместо абсолюта замерено и НЕ помогает:** по 18 парам процентилей выигрывает в одной, в
    девяти то же, в двух хуже; по 31 паре точек в трёх строго хуже худшей точки. Причина в числах:
    отношение выигрывает только когда оба участника уехали в одну сторону на столько же, а здесь чаще
    уезжает одно число из двух, и отношение наследует весь его уход. Идея закрыта замером, а не мнением.
- [ ] **16.0. Критерий §10.0 прогнан впервые и краснеет. ПОРЯДОК ОБРАТНЫЙ НУМЕРАЦИИ: закрыть раньше
  16.3 нельзя.** Снять два последних класса можно только после того, как уровень `crumb2` вывезен заново,
  иначе он не собирается. Поэтому задача делится: **эталон** правится сейчас, **удаление классов** идёт
  за 16.3. Найдено разбором 25.09.2026, нумерация вводит в заблуждение.
  Заодно пересчитан объём, и он меньше записанного: переносить в движок нечего, образец и все четыре
  текста уже стоят на `$bog_gamengine_scene` с движковым tilemap. От старого класса осталось **три**
  потребителя — текст эталона в `case/case.ts`, сгенерированный `case.view.tree` и первая строка
  `crumb2/level`, — плюс `sample_hero` на тех же двух файлах эталона. Значит это «снять два мёртвых
  класса», час, а не день. Условие: **оба класса удаляются тем же коммитом, что правит эталон**, иначе
  тесты позеленеют, а мёртвые классы останутся стоять.
  Предсказанная поломка, которую механическая регенерация пройдёт насквозь: `case.node.test.ts` держит
  `case_source.includes( '\t\t\tpos / 3.25 0 0\n' )` с **тремя табами**. Перенос узла внутрь `kids`
  добавит уровень отступа, и утверждение станет ложным **на верном документе**, без всякого
  переименования. Переводить надо на смысл, а не на новое число табов: там сторожится «редактор кладёт
  позицию списком, а такой литерал сборку MAM не проходит». В бандле
  `bog/crumb2/app` двадцать шесть вхождений `bog_gamestudio`, все `bog_gamestudio_sample*`, причина в
  первой строке вывезенного уровня: `$bog_crumb2_level $bog_gamestudio_sample_map`. Вывозитель не
  виноват, база редактора стояла уже в документе. Подробности и следствия в §10.0.
  - [x] **Переносить карту в движок НЕ надо, отменено замером 25.09.2026.** Задача ставилась так:
    `$bog_gamestudio_sample_map` это вторая, наивная реализация движкового `$bog_gamengine_tilemap`,
    значит перенести. Замер на настоящей карте `crumb2` (9 на 12, тот же атлас и та же палитра) показал,
    что переносить нечего: движковый tilemap покрывает работу образца **целиком**. Сто восемь клеток к
    ста восьми, расхождение позиции ноль, расхождение слоя атласа ноль, `fill` на кадр **0.87 мкс против
    59.2**, один узел против ста восьми живых спрайтов. Всё, чем пользуется уровень, есть; у движка вдобавок
    `size`, `tint`, `aabb` для отсечения и `props` для панели. Не хватает только записи одного символа
    (не нужна: кисть собирает строку целиком) и спрайта на клетку (движок так не делает сознательно).
    Разрыв в 68 раз объясняется тем, что у образца на каждую клетку живой спрайт с мемоизацией, и эти
    спрайты **не попадают в `scene.nodes()`** — значит их не видят ни `hidden`, ни роли, ни снимок.
    Лечение поэтому не в движке: корень образца становится чистой сценой, а уровень вывозится заново.
  - [x] **Попутно найден баг, который ломал ЛЮБОЙ новый документ, а не только вывоз.** Миры палитры
    писали `Tile … map <= map`, то есть привязку к свойству `map` на корне, а оно есть только у
    `sample_map`. Ходок, поставленный мышью в чистую сцену движка, давал документ, который собирается и
    падает на `this.map is not a function`. Жило бы до первого человека, начавшего не с образца.
    Починено: мир не выдумывает плитку, а привязывает `tile <= Tile` только если плитка в документе уже
    есть; проверено, что без плитки шаг тела работает (`move` принимает `null`) и навигационная сетка
    вырождается в пустую без броска.
    **Общая форма этой ошибки: привязка, написанная против класса, который просто оказался корнем по
    умолчанию.** Пока все начинают с образца, она незаметна. Правило: шаблон палитры привязывается
    только к тому, что объявляет сам, либо к тому, чьё наличие проверил. Нашлось мышью, а не чтением:
    два теста в наборе звали свой документ «чистой сценой», а стояли на классе редактора.
  - [ ] **Пункт «Карта» в палитре.** Своей карты в палитре нет, поэтому новый документ нечем засеять
    для кисти. Пункт объявляет `$bog_gamengine_tilemap`, карту пустой строкой, атлас привязывает только
    если атлас в документе есть. Без атласа клетки рисуются нетекстурированными — это законное
    состояние, и в инспекторе должно быть видно, что атласа нет, иначе серые клетки читаются как
    поломка. Ничего не выдумывать: ни стартовой комнаты, ни пути к картинке.
  - [ ] **Эталоны `case` переснять ТЕМ ЖЕ коммитом, что меняет корень образца.** `case/case.view.tree`
    и `case/case.view.ts` лежат в репозитории эталоном и сверяются **побайтово** с тем, что генерирует
    `doc.module()`. Обе наследуют `$bog_gamestudio_sample_map` и держат `palette`, `map`, `atlas` руками.
    Значит как только корень образца станет чистой сценой с движковым tilemap, эти два файла разойдутся
    с генератором, и тест «вывезенный модуль равен собранному в этой папке» ляжет. Переснимать надо тем
    же коммитом, иначе красное всплывёт у того, кто про эталоны не знал. Заодно `case.node.test.ts`
    читает фикстуры с диска по пути пака, поэтому путь, константа в исходнике и аргумент
    `doc.module( '$…_case' )` переименовываются вместе.
  - [ ] **`bog/crumb2` не править руками, а вывезти заново.** Соблазн поменять первую строку и
    добавить `Tilemap $bog_gamengine_tilemap`, как в `bog/crumb`, — но `level.view.tree` это **вывоз
    редактора, а не исходник**. Руками поправленный вывоз перестаёт быть доказательством пути «мышью →
    модуль → игра», ради которого crumb2 и сделан, и дописанные строки попадают в тот самый счёт
    рукописных строк, который мы этим путём и мерим. Поэтому порядок обратный: сначала образец
    редактора начинает с движкового tilemap, потом уровень вывозится заново, и только тогда греп
    что-то значит. Приёмка — греп в ноль **и живой запуск**: у уровня меняется структура, а не одна
    строка, поэтому зелёный аудит тут не значит ничего.
    Следствие для расписания: 16.0 упирается в те же занятые файлы редактора, что и 16.2, и раньше их
    не делается.
  - [ ] **Прогонять критерий, а не помнить о нём.** Правило записано давно, прогнано впервые сегодня
    и сразу красное. Место прогона — приёмка вывоза, рядом с проверкой запуска.
  - [ ] **Переписать сам критерий, а не только имя в нём (к 16.2).** Мало заменить `$bog_gamestudio_`
    на `$bog_gamengine_studio_`: греп **по бандлу** после переезда не работает вообще. Замерено — у
    `demo/jumper/-/web.js` сто тридцать два вхождения шутера, потому что подмодуль принудительно тянет
    родителя, а родитель `demo` это книга со всеми страницами. Значит критерий применяется к
    **исходнику вывезенного модуля** (`level.view.tree` и сгенерированный `.ts`), а не к бандлу.
    Подробности и контрольный замер в §10.0.
  Порядок важен: пока `bog/crumb2` снаружи, нарушение видно гребом по двум разным пакам; после 16.3
  оба окажутся в одном репозитории, и ошибку станет легче не заметить глазами.
- [ ] **16.1. Три игры-образца в демку.** `bog/jumper`, `bog/shooter`, `bog/legion` →
  `bog/gamengine/demo/<имя>`, каждая страницей книги демо, а не отдельным приложением: у пака один
  `index.html`, и второго не заводим. Старые папки удаляются только после проверки, что история
  попала в движок.
  **Порядок оказался обратным тому, что я предписал, и обратный лучше.** Я написал «`subtree add`,
  переименование следующим коммитом»; сделано наоборот: сначала переименование и снятие своей обвязки
  (`LICENSE`, свой workflow, префикс `app/`) **в исходной репе**, потом `git subtree add`. Разница не
  косметическая: в исходной репе git видит `{app/sound => sound}/jump.wav`, то есть переименования
  остаются переименованиями, и `--follow` через них проходит. При моём порядке переименование стало бы
  коммитом движка, и след оборвался бы на графте.
  - [x] **Прыгун. Принят 25.09.2026.** Ноль вхождений `$bog_jumper`, пять новых имён в дереве,
    аудит зелёный, 828 тестов зелёные, 114 вхождений `bog_gamengine_demo_jumper` в `web.js`. История
    сохранена: коммит переноса с двумя родителями, пять коммитов ветви прыгуна вплоть до первого,
    первый доступен в движке и попал в `origin/main`. Каталог `bog/jumper` удалён — история проверена
    до удаления, так что потери нет, но порядок был нарушен: сначала проверка, потом удаление.
    **`git log -- demo/jumper` печатает один коммит и выглядит как потеря истории** — фильтр по пути
    не идёт через графт, смотреть надо родителей коммита переноса и `rev-list --count`.
  - **Старые пути в журнале править НЕ надо, и это решение, а не недоделка.** В плане тринадцать
    ссылок на `bog/jumper`, `bog/shooter`, `bog/legion`. Те, что стоят в записях о принятых шагах
    («платформер `bog/jumper`, 867 строк, принят 23.09»), описывают состояние на тот день, и переписать
    их новыми путями значит подделать запись. Поправлены только **открытые** задачи, где путь
    исполняется, а не цитируется: 15.11 указывала на `bog/jumper/hero`, которого больше нет.
    Правило на будущее: журнал хранит адреса того времени, задачи — адреса сегодняшнего дня.
- [ ] **16.2. Редактор в движок.** `bog/gamestudio` → `bog/gamengine/studio`, отдельной страницей
  книги демо, тогда ссылка будет `#!demo=studio` и второй `index.html` не нужен. **После** того как
  закончатся начатые в редакторе работы (роли, тайловые инструменты, кисть), иначе перенос столкнётся
  с чужими правками.
  **Предполётная проверка сделана 25.09.2026, переезду ничто не мешает, кроме занятости файлов.**
  Проверялось то единственное, что способно сорвать сборку в CI после переноса, — зависимости пака:
  у редактора их сорок с лишним, из них все `$mol_*`, свои `$bog_gamengine_*`, плюс `bog/probe`
  (remote есть, движок уже собирает его в CI) и `giper/baza`. По базе сверено содержимое, а не ветка:
  локальное дерево совпадает с опубликованным `giper-dev/baza` до нуля строк, значит CI склонирует
  ровно то, на чём редактор работает. `$bog_myapp_level` в тесте и в подсказке — не зависимость, а
  литерал, и первый сегмент у него настоящий пак, поэтому сборку он не рвёт.
  Заодно получено прямое подтверждение причины этапа: **у `bog/gamestudio` нет remote вообще и нет
  ни одного workflow.** То есть ссылки на редактор не существует не потому, что её забыли сделать, а
  потому что снаружи движка её сделать нечем. Перенос — единственный путь к ней, а не наведение порядка.
- [ ] **16.3. Игры, собранные через редактор, в демку.** `bog/crumb` и `bog/crumb2` туда же. Вторая
  это проверка пути «мышью → модуль → игра», её стоит сохранить именно как проверку, а не как игру.

- [ ] **15.24. Кисть жива только пока открыта её вкладка, и видимое состояние об этом молчит.**
  Замерено 25.09.2026 отдельно от правок: армируешь кисть (галки тайла и инструмента стоят),
  переключаешься на «Ассеты», возвращаешься на «Тайлы» — галки по-прежнему стоят, а клик **не красит**.
  Без переключения вкладки те же клики красят. Причина в том, что колода рисует ровно одну страницу,
  поэтому уход с вкладки уносит из DOM то, на чём держится кисть.
  **Это меняет прежнее решение по 15.10.** Тогда одностраничная колода была отнесена к косметике
  (дерево мигает на кадр при монтировании) и признана не стоящей получаса. Теперь у неё второе
  следствие, и оно не косметическое: **инструмент показывает себя готовым и не работает.** Это тот же
  класс, что закрывала 15.22, только уровнем ниже, и именно оно объясняет, почему первый проход пути
  положил спрайт вместо стены.
  Лечится другим компонентом в левой колонке — книгой вместо колоды либо колодой, держащей страницы
  смонтированными. Приёмка живьём и с переключением вкладок, а не только кликами на одной.

- [ ] **15.25. Редактор умеет, а вывоз не отдаёт.** Отдельный класс долга, и он дешевле остальных:
  писать ничего не надо, надо перестать терять. Найден 25.09.2026 при подготовке перезамера, когда
  каждую руками дописанную строку `bog/crumb2` заставили назвать отсутствующую возможность редактора.
  - **Восстановление начального состояния.** Кнопка «Стоп» откатывает сцену: `app.snapshot()` обходит
    `Doc().nodes()`, снимает значения через `props_of( path )` и пишет их назад, опираясь на движковый
    `node.props()`. Игра хочет того же и пишет руками: в `crumb2` это 10 строк `restart()`, которые
    ставят герою позицию, гасят скорость и сбрасывают часы. **Не путать с `scene.snapshot()`** — это
    `Float32Array` по три числа на узел для кадра, к восстановлению свойств он отношения не имеет, и
    ссылка на него уводит читателя не туда.
  - **Обвязка документа.** Редактор связывает сцену с часами и вводом сам (`doc.scene()` ставит
    `scene.clock` и `scene.input`), а вывоз отдаёт только документ, поэтому каждая игра пишет ту же
    связку заново.
  - **Камера.** Редактор умеет «Показать всё» (`fit()` и `$bog_gamestudio_app_bounds`) и знает размеры
    карты, а вывоз камеры не отдаёт вовсе: в `crumb2` это блок камеры плюс 5 строк центровки по карте.
  - **Пауза.** У редактора пауза часов есть для режима правки, игра пишет её руками.
  Не теряется, для полноты: роль (15.23) и словарь звуков живут в документе и уезжают сами.
  **Замер обёртки по четырём играм, 25.09.2026, чтобы это было числом, а не мнением.** Скелет один и
  тот же у `crumb2`, прыгуна, шутера и стратегии: `Draw` со `scene` и `cam`, `Clock`, `Input` с
  `key <= Key`, `Scene` со `clock`, `input` и `cam`, кнопка «Ещё раз», экран конца. Различается
  содержимое, а не форма: имена действий в `bind *` (у прыгуна `jump`, у шутера `forward` и `back`),
  вид камеры (`cam_flat` у трёх, `cam_deep` у шутера), HUD и правила. Отсюда вывод про `index.html` и
  обёртку из протокола перезамера: **вывозим скелет со слотами, а не обёртку целиком.** Раскладка
  клавиш остаётся игре — она честно её, а не потерянная возможность редактора.

- [x] **15.26. Перезамер пути мышью. СДЕЛАН 25.09.2026.** Уровень `bog/crumb2` собран заново мышью в
  переехавшей студии, вывезен модулем, положен в игру, игра запущена и играется. Вкладка «Исходник»
  не открывалась ни разу: **ноль честно вынужденных обращений**, весь документ набран щелчками.
  **Три ведомости, охват объявлен до замера.**
  - **Долг редактора — 34 счётных строки.** Обвязка документа 9 (`Scene` со `clock`, `input`, `cam`,
    `auto_nodes`, блоки `Clock` и `Input`): редактор ставит это сцене сам в `doc.scene()`, игра пишет
    заново. Камера 7 (`Cam`, `height`, `pos <= cam_pos` и мемо центровки по карте): у редактора есть
    «Показать всё» и размеры карты, вывоз камеры не отдаёт. Обращение к узлам 4: роль работает, но
    отдаёт узел без типа, и тело приводится к `$bog_gamengine_phys_walker` руками. Крючок для узла
    игры 2: у вывезенного уровня его нет, а `auto_nodes` в вывозе не назван. Перезапуск 6: редактор
    умеет откат состояния кнопкой «Стоп», вывоз его не отдаёт, и стартовая точка героя вписана в игру
    числом. Шаг сцены 3: вывезенная сцена не шагает сама, `Scene().step()` дёргается из строк HUD.
    Пауза 3: у редактора пауза часов есть, вывоз её не отдаёт.
  - **Цена пака — 104 счётных строки.** Страница с HUD, экран конца, кнопка «Ещё раз», раскладка
    клавиш (14 строк имён действий — честно игры), стили холста, `index.html`. Скелет совпадает у
    четырёх игр, содержимое нет.
  - **Решено кодом — 57 счётных строк** правил (`rule/rule.ts`, 93 сырых) по решению 10.1, плюс блок
    `Rule` в дереве. Тесты вне счёта: `app.test.ts` 73 строки, `rule.test.ts` 63.
  - **В вывезенном модуле правок нуль.** `diff` сохранённой до правок выгрузки против того, что лежит
    в игре, пуст: 63 строки дерева и 42 строки спутника целиком из редактора.
  **Сравнение с прошлым — и оно не в пользу «стало меньше».** Сырых строк руками теперь 181 против
  прежних 166 и 272 у `bog/crumb`. Выросло потому, что **прошлая игра не работала, а мы этого не
  знали**: замерено на живой странице, что при `kids <= nodes /` уровень в сцену не попадает вовсе (у
  старой игры один узел в сцене, один батч из 108 клеток карты, ноль крошек), холст без стилей имел
  нулевую высоту, а монтирование в `<div>` лишало игру клавиатуры — плагин висит на узле приложения, а
  событие от `<body>` вверх до него не доходит. Три правки на это (крючок `auto_nodes`, файл стилей 22
  строки, монтирование на `<body>`) и дали рост. То есть честное сравнение звучит так: **166 строк
  описывали игру, которая не рисовала поставленное мышью и не слушала клавиатуру в своём паке; 181
  строка описывает игру, которая играется, и долг редактора внутри неё 34.**
  **Роли и двусторонний вывоз подтвердились числом.** Обращение по роли ужало `hero()` и `crumbs()` с
  десяти строк до четырёх, правки в вывезенном модуле исчезли совсем (было две), тело поставленное
  мышью поехало с первого запуска. Проверка игры на живой странице: герой ушёл с 1.5 на 2.6 по D, с
  −1.5 на −2.6 по S, счётчик крошек упал с пяти до четырёх, батчей два — один пул карты и один на пять
  крошек, то есть поставленное мышью теперь рисуется.
  **Время по часам не замерялось честно.** Путь прошёл драйвер пробы за 32 действия и 2 секунды
  машинного времени; человеческого времени это число не значит, поэтому вывод «редактором быстрее»
  им не подпирается. Сравнимая мера здесь — число действий, не секунды.
  **Состав отличается от прошлого, называю явно:** комната 6×5 вместо 12×9 и рисунок стен другой,
  потому что кисть не растит карту (см. ниже). Всё остальное то же: карта, тело героя, пять крошек,
  те же правила.

- [ ] **15.27. Четыре поломки, найденные перезамером.** Каждая ловится мышью за минуту и ни одна не
  ловится тестами.
  - **Кисть не растит карту.** `paint_all` пропускает клетки вне существующих строк, поэтому размер
    поля мышью не меняется вовсе: комнату больше той, что дала палитра, собрать нельзя.
  - **«Карта» из палитры получает `pos` от клика.** Клетки рисуются со сдвигом, а `cell_at` считает
    от начала координат, поэтому картинка и проходимость разъезжаются молча; лечится обнулением `pos`
    в инспекторе, то есть пользователь должен знать про грабли. Ставить карту в начало координат или
    учить тайлмап смещению.
  - **Удаление тела оставляет ссылку в мире физики.** После удаления «Героя» в документе остаётся
    `bodies / <= Hero`, сцена собирается в **ноль узлов** (замерено), и мышью это не лечится:
    объявления `Phys`, `Tile`, `Atlas` не удаляются, пока на них ссылаются, а свойств в инспекторе у
    них нет вовсе. Это тот самый вариант (г) из 15.3: снимать надо постановку, а не узел.
  - **Пять одинаковых заголовков.** Узлы, поставленные ассетом, все называются `coin`, и в списке
    сцены различить их нельзя — выбирать приходится щелчком по холсту. Роль ставится каждому вручную.

## Этап 17. Подозрения на двойной счёт

Список собран 25.09.2026 исполнителем, который закрывал четыре копии обратного хода сетки. **Это
подозрения, а не проверенные факты**, и записаны они так нарочно: ни одно не замерено, ранжировано по
тому, насколько автор в нём уверен. Правило, из которого список вырос, лежит в STYLE.md: копия
преобразования сходится с исходным только там, где оба тривиальны.

- [ ] **17.1. Самое сильное: ручная проверка свежести, у которой набор входов поддерживается руками.**
  `tilemap.fresh()` помнит строку карты, размер, палитру, мир, тинт и теперь начало сетки, а `emit()`
  отдаёт прежний пул, если всё совпало. Это вторая реализация того, что реактивная система делает сама, —
  **и она уже отказала ровно тем способом, каким обязана была**: начало сетки стало новым входом
  отрисовки, и никто не добавил его в `fresh`, отчего рисунок молча отставал. Каждый будущий вход в
  `emit` придётся помнить дважды. Второй экземпляр той же формы — `text/text.ts:113` со своим
  `fresh( value, axes, height, align, color )`.
  **Ограничение, без которого лечение будет хуже болезни:** пул батча живёт в горячей зоне намеренно, и
  завести `@$mol_mem` на `emit` значит внести в кадр цену холодной зоны, около 1.3 мкс на объект
  (раздел 1 архитектуры). Значит задача не «перевести на мемо», а **сделать забывание невозможным, не
  выходя из горячей зоны**. Форму выбирает исполнитель и обосновывает замером кадра до и после.
  **ЗАКРЫТО 25.09.2026, и подозрение оказалось шестью живыми багами.** Не хватало атласа, **содержимого**
  атласа и плоскости в тайлмапе; шрифта, **содержимого** шрифта и атласа в тексте. Каждый подтверждён
  красным тестом до правки. Лечение: `$bog_gamengine_watch`, вход получается **через** наблюдателя
  (`watch.of( … )` возвращает значение), поэтому сравниваемый набор и читаемый набор — один текст на
  одной строке. Механизм **дешевле** заменённого на 7% (восемь сравнений по личности против двадцати двух
  поэлементных с копированием), плюс 88 нс — цена самой правильности, итог +60 нс на узел в кадре, около
  десятой процента; грязный путь на 3% быстрее.
  **Структурно забывание закрыть нельзя** — чтение мимо наблюдателя никто не запретит, а прокси и мемо
  стоят кадра. **Единственный работающий страж — перечень тестов, по одному на вход:** проверка на
  количество входов не годится, потому что забытое чтение число наблюдаемых **не меняет**, зато
  правильно добавленный вход её ломает — то есть счётчик наказывает за аккуратность и молчит о
  небрежности. Сейчас восемь входов тайлмапа и одиннадцать входов текста, у каждого тест, что изменение
  этого входа меняет вывод; часть — обходом по `props()`, где новое свойство обязано попасть в список
  рисующих или в названный список холостых, иначе тест краснеет.
  **И правило, выведенное из трёх повторов: наблюдать значение, а не держателя.** Личность атласа, плитки
  и шрифта не меняется, когда инспектор правит их **на месте**, поэтому смотреть надо на `names()`,
  `map()`/`plane()`/`origin()`, `family()`/`size()`/`chars()`. Замером показано, что после перехода на
  значения watch держателя не просто недостаточен, он **избыточен**: снятие watch с `tile` и с `atlas`
  не уронило ни одного из 570 тестов. Держатель при этом опаснее избыточности — он даёт **видимость**
  покрытия, ловя подмену и пропуская правку на месте, ровно как было с атласом.
- [x] **17.2. ЗАКРЫТО КАК МНИМОЕ 25.09.2026, и запись ниже была неверна в обе стороны.** Замер: тайлмап
  3×2, `size` 1. Без поворота формулы расходятся на `(√½ − ½)·size = 0.2071` на сторону, а при повороте
  ровно 45° по Z в X и Y **совпадают** — то есть «сходятся, пока нет поворота» неверно дважды.
  **Но это не дефект, а разные вопросы, и это главное.** `emit()` пишет коробку **на клетку** для
  отсечения в `batch.fill`, и описанная сфера там правильна: она не зависит от поворота и стоит одно
  умножение, тогда как тесная коробка требовала бы четырёх переносов углов на клетку каждый кадр.
  `aabb()` — тесная коробка **на узел** для выбора и подгонки кадра. Во всех замеренных состояниях
  объединение `emit` **содержит** `aabb`, и иначе быть не может: радиус берёт `scale_max`, наибольший
  масштаб по осям, значит дешёвая коробка никогда не отрежет того, что тесная считает видимым.
  Правильная формулировка на будущее: **не сходятся и не должны — одна на клетку для отсечения, другая
  на узел для выбора, и первая всегда содержит вторую.**
- [ ] **17.2а. (прежняя запись, сохранена как пример неверного подозрения.)** `tilemap.emit()` считает
  коробку экземпляра как `wx ± radius`, где радиус это описанная сфера (`size * scale_max * SQRT1_2`), а
  `tilemap.aabb()` считает коробку всей карты переносом четырёх углов с `half = size/2`. Один вопрос, два
  ответа, и сходятся они пока у мира нет поворота. Начало сетки в `aabb()` уже поправлено, обе формулы
  оставлены как были.
- [ ] **17.3. ПОДТВЕРЖДЕНО ЗАМЕРОМ 25.09.2026, и беда не в `0.5`, а в потерянной матрице мира. Мест
  догадки ПЯТЬ, а не одно.** Запасной путь `$bog_gamengine_studio_bounds` это `world[12] ± size/2`:
  берётся только перенос, **поворот и масштаб мира не смотрятся вовсе**. Состояния достижимые — `pos`,
  `rot`, `scale` стоят полями инспектора и правятся мышью.

  | узел | рамка выделения | настоящая протяжённость |
  |---|---|---|
  | спрайт 1×1 | 1×1 | 1×1 |
  | спрайт 1×1, масштаб 10 | 1×1 | 10×10, рамка **в десять раз меньше** |
  | спрайт 1×1, масштаб 0.1 | 1×1 | 0.1×0.1, рамка **в десять раз больше** |
  | спрайт 4×0.5, поворот 90° по Z | 4×0.5 | 0.5×4, рамка **транспонирована** |

  **Узлов без `size()` — 29 из 45**, а `aabb()` есть ровно у одного класса (тайлмап), значит через
  запасной путь идут 44 из 45, и 29 получают догаданную коробку 1×1. Среди них **нерисуемые**: `cam`,
  `cam_flat`, `cam_deep`, `cam_edge`, `light`, `scene`, `nav_agent`, все девять `brain_*`. Замер эффекта
  на «Показать всё»: один спрайт даёт рамку `−0.5 −0.5 0.5 0.5`, тот же спрайт с камерой на `x = 20` даёт
  `−0.5 −0.5 20.5 0.5` — подгонка отъезжает, чтобы вместить пустоту.
  **Ещё два места с ДРУГИМИ умолчаниями на тот же вопрос:** `point.pick` при отсутствии размера берёт
  `0.5`, а `point.pick_box` берёт `0`, и оба тоже не смотрят на поворот и масштаб. Это живёт в выпущенной
  стратегии: у `demo_legion_unit` нет `size()`, он объявляет `radius() 0.35`, а рисуется спрайтом
  0.85×0.85 — клик берёт юнита на расстоянии до 0.49 мира, то есть **шире нарисованного**, а рамка берёт
  только если внутри её **центр**, поэтому рамка, накрывшая правую половину спрайта, не берёт никого.
  **Направление лечения (решение, не догадка): отсутствие протяжённости должно быть выразимо, а не
  подменяемо.** У камеры, света, мозга и сцены протяжённости нет, и коробка 1×1 у них хуже отсутствия:
  «Показать всё» подгоняется под пустоту. Это та же подмена, что `skip || ok` и `|| [ 0, 0 ]` — умолчание
  вместо отсутствия. Один источник правды на вопрос «протяжённость узла», считающий от матрицы мира и
  размера, и **пустой ответ для узлов без протяжённости**, который потребители обязаны пропускать, а не
  подставлять за него число.
- [x] **17.4. ЗАКРЫТО КАК МНИМОЕ 25.09.2026 в исходной формулировке: «проходимость хранится дважды» —
  не тот вопрос.** `nav/grid.solid()` это `$mol_mem`, который выделяет **свежий** `Uint8Array` из плитки
  на каждый пересчёт, а не правит существующий на месте. Поэтому пути, на котором выжило бы
  полуобновлённое зеркало, не существует: либо мемо годно и зеркало соответствует плитке, из которой
  собрано, либо негодно и следующее чтение пересобирает всё. Рост и сжатие не требуют отдельной
  обработки по той же причине — размер массива это **следствие** пересборки, а не состояние, которое
  кто-то ведёт. Замерено 5×3 → 7×5 → 3×3: размеры и значения клеток следуют за плиткой, поиск пути
  работает по выросшей области. Черновые массивы в `grow()` только растут и не сжимаются, но в них лежат
  индексы кучи, а не проходимость, — лишний размер безвреден. Разделение «нав читает зеркало, физика
  читает плитку» — намеренное мягкое блокирование для поиска пути, и это не дефект.
  **Вывод шире этого пункта: реактивная система делала работу, которую подозрение приписало рукам.**
  Ровно противоположно 17.1, где руками делали то, что система делает сама. Значит вопрос к зеркалу не
  «сколько копий», а **«пересобирается целиком или правится на месте»**: пересборка целиком забывание
  исключает, правка на месте его приглашает.
- [ ] **17.4б. Переформулировано: у блокировки нет названного срока жизни.** `block()` пишет в то самое
  зеркало, которое пересобирается с нуля, значит **каждая блокировка молча стирается следующей правкой
  карты** — без предупреждения, без возвращаемого значения, и наблюдать это можно только по тому, что
  закрытая клетка снова проходима. До этой смены состояние было почти недостижимо, потому что карты во
  время игры не менялись. **Теперь достижимо:** кисть студии растит и правит карту в любой момент.
  Вопрос к решению: сколько живёт блокировка и кто вправе её отменить. Рамку «два источника правды»
  снять, она вела не туда.

  **ЗАМЕРЕНО 25.09.2026, и вопрос о сроке жизни решать нечем: у `block()` НОЛЬ потребителей.** Во всём
  дереве два вызова, оба в её собственном тесте; остальные попадания грепа — однофамильцы из `studio/doc`
  и `bog/vmap`. Дефект настоящий, **цена его сегодня ровно нулевая**, потому что стирание некому
  заметить. Отсюда формулировка, которой не было: **срок жизни надо назвать ДО появления первого
  потребителя**, иначе первый прочтёт имя как обещание долговечности — это чтение приглашает само имя.
  Достижимость сужена замером: единственный непрограммный писатель карты — кисть студии
  (`studio.view.ts`), выпущенные игры объявляют карту литералом и в рантайме не пишут. Значит стирание —
  грабля разработчика, а не игрока. Форма лечения выбрана (блокировки **вне** пересобираемого зеркала,
  накладываются после пересборки: срок жизни становится явным по построению, а не объявляется вопросом),
  но правка ждёт решения пользователя, потому что спешки при нулевой цене нет.
- [x] **17.4в. Цена пересборки зеркала: числа кадра были получены на несуществующем входе, и это
  поправлено до вынесения.** Первый замер дал 1.4 мс на карте 100×100 и 9.1 мс на 256×256 — «девять
  процентов кадра на каждую правку карты». **Обе половины неверны.** Карт таких размеров в проекте нет:
  карта студии 6×5 = 30 клеток, из палитры 6×6 = 36, самая большая в проекте — `demo/legion` 23×40 = 920
  клеток, и та литерал, в рантайме не меняется. При 141 нс на клетку это 4 мкс и 130 мкс. Сверх того у
  студии **по умолчанию сетки нав нет вовсе**, она появляется только если поставить агента из палитры.
  И «на каждую правку» тоже неверно: зеркало **полностью ленивое**, пересборок столько, сколько
  чередований «испортили — прочитали». Замер: десять правок с одним чтением в конце — **одна**
  пересборка, с чтением после каждой — десять, без чтения — ноль. Значит залипающей мыши нет.
  **Стойкая находка — цена на клетку, и она не в пересборке.** Каждая клетка платит за **два чтения
  мемо**, потому что `tile.cell()` читает `rows()` и `solid()` на каждый вызов, 86 нс. Подъём двух чтений
  из цикла даёт **14.5×** на десяти тысячах клеток (858 → 59 мкс) **без отказа от пересборки целиком**,
  то есть без потери того свойства, ради которого зеркало и не может протухнуть. Вариант с таблицей на
  128 байт и `charCodeAt` даёт 49× и **отклонён**: он держится на том, что символы карты укладываются в
  семь бит, а карты пользовательские. Инкрементальность отклонена по той же причине — она обменивает
  невозможность протухания на меньший выигрыш, чем даёт подъём даром.
  **Правило для себя: «процент» без названного знаменателя — не число.** Лишний проход по клеткам был
  назван «+0.3–0.7%», и это процент **от пересборки**, а читалось как процент от кадра; тем же способом
  за день была занижена в девять раз оценка цены тайпчека.
- [x] **17.4г. ЗАКРЫТО 25.09.2026: сглаживание починено, сторож дальнего приказа стоит в CI со своим
  порогом, запас измерен ДВУМЯ точками с раннера, и числа собираются сами каждым прогоном.**

  | | точка 1 | точка 2 | своя машина |
  |---|---|---|---|
  | худший кадр | 34.5 | **29.7** | 9.4–9.9 |
  | средний | 12.33 | 12.05 | 4.5 |
  | перепланирований за 90 кадров | 1521 | **1766** | ~1000–1200 |

  **Запас порога 60: от 1.8× до 2.0×**, а разброс самих точек 16 % — вчетверо меньше запаса, поэтому порог
  держится не на везении. Одна точка была наблюдением, две со сходящимся разбросом — основание.
  **Отношение машин 3.2–3.7×, а не 2.2**: кто захочет перенести местный порог, обязан брать диапазон, а не
  одно число, и помнить, откуда диапазон берётся (см. ниже про таймер).
  **Привязка проверки СРЕДНЕГО кадра к назначенной машине теперь обоснована замером, а не осторожностью:**
  средний на раннере **12.05 мс при бюджете 8**, то есть безусловной она краснела бы всегда.
  **Риск, названный числом: порог 60 держится, пока раннер не станет ещё медленнее, и первым признаком
  будет не кадр, а число перепланирований.** Оно уже растёт от прогона к прогону — 1521, затем 1766, — по
  той же причине: медленнее кадры, больше игрового времени в окне, больше срабатываний таймера. Значит
  цена идёт вверх **быстрее, чем линейно** от замедления, и следить надо за столбцом перепланирований в
  `-/far.md`: он поедет раньше, чем кадр упрётся в порог.

  История замеров ниже — она объясняет, почему критерий один, а ответов было три. Переносимость решает
  **запас, а не число**, и за
  день критерий дал три разных ответа, потому что дважды применялся не к тому числу. Цена была 181 мс,
  порог назвали по факту (200) с запасом **10 %** и привязали к назначенной машине. Починка `visible()`
  внутри сглаживания (`tile.line_free`, коммит `755c469`) опустила худший кадр того же сценария до
  **9.0–9.9 мс** (три прогона: 9.0, 9.7, 9.3 при 988–998 перепланированиях, средний 4.51–4.57), запас при
  пороге 24 стал **2.4×**, привязку сняли, и прогон CI на `2562bc1` вышел **зелёным** — сторож дальнего
  приказа в CI работает.
  **Чего зелёный прогон тогда не дал: числа.** Было известно только «меньше 24», а двадцать там или десять
  — нет, потому что **вывод дочернего процесса сборка печатает лишь при падении**: `mam` зовёт
  тестовый бандл через `$mol_run.spawn`, тот при незаданной `MOL_RUN_ASYNC` идёт синхронной ветвью
  `spawnSync( app, args, { shell, cwd, env } )` **без `stdio`** (по умолчанию `pipe`), а захваченный
  `res.stdout` попадает только в текст ошибки. Проверено прогоном: та же `spawnSync` без `stdio` строку
  глотает и держит в `res.stdout`, со `stdio: [ 'pipe', 'inherit', 'inherit' ]` — печатает. Косвенное
  подтверждение из лога: тесты модуля шли 43 секунды, то есть оба браузерных сценария отработали, а ни
  одной строки `проба:` в логе нет.
  **Число с раннера пришло, и оно красное: худший кадр 34.5 мс при пороге 24.** Отсюда главное измерение
  вечера: **отношение машин для сценария с перепланированием не 2.2, а 3.6, и оно не константа.**

  | | своя машина | раннер | отношение |
  |---|---|---|---|
  | худший кадр | 9.3–9.9 | **34.5** | **3.6×** |
  | средний | 4.5 | 12.3 | 2.7× |
  | перепланирований за 90 кадров | ~1000 | **1521** | **1.5×** |

  Причина в том, что на медленной машине **растёт сама работа**: перепланирование запускается по игровому
  времени (`since >= replan()`, раз в полсекунды), а не по кадрам, поэтому медленные кадры дают больше
  срабатываний таймера в том же окне из 90 кадров — 1521 против тысячи. Цена множится на цену: считает
  медленнее **и** считает чаще. Множитель 2.2 был снят на обычной пробе стратегии, где перепланирований
  почти нет, и на этот сценарий не переносится. **Правило: множитель машин, снятый на другом сценарии, для
  порога негоден** — у сценария со своим таймером отношение зависит от скорости кадра, а не только от
  скорости машины. Это относится ко всякому нашему порогу, снятому локально.
  **Решение: два порога, каждый со своей причиной.** Местный 24 для назначенной машины; для раннера
  отдельная константа `far_runner_max` = **60**, то есть 1.7× от измеренных 34.5 — разбросом раннера не
  объясняется, — и при этом ловит ту регрессию, ради которой сторож ставился: до починки сценарий давал
  181, а удвоение нынешней цены даст 69, тоже выше порога. В тексте отказа печатается, **какой из двух**
  порогов пробит, и в отчёт шага уходит пометка, где мерилось.
  **Поэтому числа теперь пишутся в отчёт шага:** `$bog_probe_step_add` дописывает строку таблицы в файл из
  `GITHUB_STEP_SUMMARY` — сценарий, худший кадр, средний, перепланирований, порог, — шапка один раз на
  файл, при отсутствии переменной и при недоступном пути молчит и не падает. Это добавка к `say`, а не
  замена: число выживает на **зелёном**. И у сводки нашлась своя граница: она видна человеку на странице
  прогона, но **в API её нет и между шагами она не переносится** — у каждого шага свой файл. Поэтому строка
  пишется ещё и в `demo/legion/probe/-/far.md`, откуда её печатает отдельный шаг с `if: always()`; так
  вторая и третья точки собираются сами, без чтения страниц глазами.
  **И честная граница нынешнего порога: он ловит рост сглаживания в 6 раз, а не в 2** (рост втрое —
  16.4 мс и зелёное, в шесть раз — 25.2 и красное), потому что после починки сглаживание это лишь около
  трети худшего кадра. Плотный сторож для этого дефекта надо ставить на **цену перепланирования**, а не на
  кадр целиком.
  **Что осталось в силе целиком: отсутствие числа — названный отказ.** Негативный прогон нашёл дыру в
  самом стороже: пропавшее число сворачивалось в ноль через `reduce` с начальным нулём (`NaN > 0` ложно),
  проверка на конечность его не видела, и прогон был зелёным. Теперь худший кадр собирается только по
  прочитанным кадрам, а их число сверяется с длиной окна, и обе поломки краснеют в обоих режимах.
  **И честная граница нового порога: он ловит рост сглаживания в 6 раз, а не в 2.** Замер ростом в копии
  страницы: втрое дороже — 16.4 мс, зелено; в шесть раз — 25.2 мс, красное. Причина в том, что после
  починки сглаживание это лишь около трети худшего кадра, поэтому запас 2.4× по кадру означает
  пятикратный запас по самому сглаживанию. Если понадобится ловить мелкий рост, сторожить надо **цену
  перепланирования**, а не кадр целиком.
  Первый замер 25.09.2026 на
  нетронутом продукте обычным способом играть: зум колесом до предела, рамка по всему холсту (50 из 50
  своих), приказ в противоположный угол карты — худший кадр **188 мс** при цели 24, средний 16.5,
  перепланирований 1173, в густейшем кадре 91. Сторож этого не видел не потому, что смотрел среднее, а
  потому, что **выпущенная проба такого сценария не играла**: она берёт 16 % холста и кликает в центр.
  Не хватало сценария, а не сторожа. Причина цены названа снятием одного вызова: без `grid.smooth()` тот
  же сценарий даёт **8 мс** при том же числе перепланирований, то есть разница в 23 раза целиком в
  сглаживании. И цена считается по **сырому** пути, по которому сглаживание ходит, а не по точкам на
  выходе: 28 сырых точек дают 188 мс, а на выходе их 7 — выходная длина цену не предсказывает вовсе,
  и оптимизировать по ней нельзя.
- [ ] **17.5. Две разные реализации билборда, найдено 25.09.2026 побочно.** У узла (`node.trans()`)
  билборд берёт **только рысканье** через `cam_yaw()`, то есть `atan2( world[8], world[10] )`. У текста
  оси нормируют **все три столбца** матрицы камеры. Отсюда наблюдаемое следствие, уже замеренное: поворот
  камеры **только по тангажу** меняет рисунок текста, оставляя `text.world()` побайтово тем же. Два ответа
  на один вопрос «как узел смотрит на камеру», сходящиеся, пока камера не наклонена.
  Найдено не чтением файлов, а поиском состояния, которое двигает камеру и не двигает текст, — и это
  отдельный вывод, записанный в STYLE.md: **изоляция входа попутно рисует настоящий граф зависимостей.**
  Что выяснить до правки: какая из двух реализаций верна по замыслу, и есть ли достижимое состояние, где
  расхождение видно человеку, а не только в матрице.

  **ЗАМЕРЕНО 25.09.2026, и реализаций оказалось ТРИ, а не две.** У частицы своя, и это арифметика текста
  дословно: тот же обход трёх столбцов с нормированием, только в буфер с шагом три вместо четырёх.

  | камера | спрайт (узел) | текст | частица |
  |---|---|---|---|
  | ровная | `0, 1, 0` | `0, 1, 0` | `0, 1, 0` |
  | тангаж 45° | `0, 1, 0` | `0, 0.707, −0.707` | `0, 0.707, −0.707` |
  | крен 30° | `0, 1, 0` | `−0.5, 0.866, 0` | `−0.5, 0.866, 0` |

  Значит болезней две сразу: **текст и частица — дубликат одного алгоритма**, а **узел — другой алгоритм**.
  У этих поведений есть имена: узел даёт **цилиндрический** билборд (только рысканье, остаётся стоймя,
  когда камера смотрит под ноги), текст и частица — **сферический** (полное выравнивание, наклоняется и
  кренится вместе с камерой). Оба защитимы по отдельности; дефект в том, что **один флаг `billboard`
  значит две разные вещи в зависимости от того, какой класс его читает**, и различие нигде не названо.

  **Дубликат ЗАКРЫТ (`241f94c`):** `$bog_gamengine_vec_mat4_basis( out, m, stride )`, раскладка буфера
  стала аргументом, обе точки вызова — одна строка. Состояние, изолирующее вход, оказалось неожиданным:
  **одного поворота камеры не хватает**, потому что у матрицы поворота столбцы уже единичной длины и
  нормирование на ней — пустая операция, которую тест не видит. Понадобился **масштаб камеры** 2, и тогда
  утверждение стало инвариантом (столбцы равны столбцам камеры, делённым на их длину), а не литералом,
  зависящим от знаков `$mol_3d`. Негативные прогоны сделаны по каждой точке отдельно: первый излом
  краснел на тесте `vec`, который идёт раньше, а `$mol_test` на нём останавливается, — то есть про точки
  вызова он не доказывал ничего.

  **Расхождение цилиндрического и сферического НЕ ТРОНУТО и ждёт решения пользователя.** Видимого сегодня
  состояния нет: в стрелялке камера наклоняется, но единственный билборд там — эмиттер частиц, он и так
  сферический; в комнате наклон есть, билбордов нет. Поэтому ни тест, ни проба это не ловят — расхождение
  латентное, и первым его увидит тот, кто поставит билбордный спрайт в наклоняемую сцену. Довод против
  слияния: цилиндрический путь **намеренно** сохраняет собственные тангаж и крен узла (`rot[0]`, `rot[2]`),
  а сферический **молча выбрасывает весь поворот узла**, оставляя только перенос, — разное обращение с
  `rot` у одного флага в одном базовом классе выглядит выбранным, а не забытым. Дешёвый первый шаг —
  назвать два вида, а не сливать их.

  **И мёртвый счёт оказался тем же дефектом, а не отдельным.** У билбордного текста `cam_yaw()` считается
  и выбрасывается: доказано замером — билбордный текст на `pos 3,4,5` при двух разных рысканьях камеры
  даёт перенос `3,4,5` **оба раза**, то есть рысканье провабельно не доходит до того, что текст берёт из
  `world()`, а поворотную часть затирают оси. Цена 544 нс на текст на движение камеры (верхняя оценка,
  машина под нагрузкой 5.9), и это не только арифметика: `$mol_3d_mat4.rotation` и `.multiply` каждый
  выделяют новый массив. **Убрать нельзя без решения выше:** отходы живут внутри `node.trans()`,
  цилиндрического пути, а текст берёт оттуда `world[12..14]`; считать позицию отдельно значит завести ещё
  одну копию преобразования — ровно ту болезнь, которую лечили. Единственный выход — чтобы узел знал, что
  этот подкласс не хочет цилиндрического билборда, то есть чтобы два вида были названы.
