# $bog_gamestudio

Редактор сцен для `bog/gamengine`. Документ сцены это текст `view.tree`, редактор компилирует его штатным компилятором мола прямо в браузере через `new Function`, поэтому странице редактора нужен `unsafe-eval`: `index.html` без `Content-Security-Policy`, а на своём хостинге в `script-src` добавляйте `'unsafe-eval'`.

Над холстом кнопки «Играть», «Пауза», «Стоп». В режиме правки часы стоят, работают гизмо и инспектор. «Играть» снимает снимок всех свойств узлов через `props()`, запускает часы и включает ввод: герой образца (`$bog_gamestudio_sample_hero`) ходит по WASD. «Стоп» останавливает часы и возвращает узлам значения из снимка, исходник при этом не меняется. Узлы, созданные или удалённые во время игры, откат не трогает. Правка исходника во время игры пересобирает сцену заново, состояние игры теряется.
